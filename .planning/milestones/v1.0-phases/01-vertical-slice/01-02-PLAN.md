---
phase: 01-vertical-slice
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/api/upload/route.ts
  - src/components/file-dropzone.tsx
  - src/components/content-preview.tsx
  - src/app/page.tsx
  - src/app/api/upload/__tests__/route.test.ts
autonomous: true
requirements: [CORE-02, CORE-03, PARSE-01]

must_haves:
  truths:
    - "User can drag-and-drop or click-to-browse to upload a .txt file"
    - "Upload route accepts .txt files and rejects non-.txt files with a 400 error"
    - "After upload, user sees a content preview with word count, line count, and transcript text"
    - "Invalid file types show an error message in the dropzone"
  artifacts:
    - path: "src/app/api/upload/route.ts"
      provides: "File upload endpoint accepting FormData"
      exports: ["POST"]
    - path: "src/components/file-dropzone.tsx"
      provides: "Drag-and-drop upload component"
      exports: ["FileDropzone"]
    - path: "src/components/content-preview.tsx"
      provides: "Parsed content preview with metadata"
      exports: ["ContentPreview"]
  key_links:
    - from: "src/components/file-dropzone.tsx"
      to: "src/app/api/upload/route.ts"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/upload"
    - from: "src/app/api/upload/route.ts"
      to: "src/lib/parsers/registry.ts"
      via: "parseFile call"
      pattern: "parseFile"
    - from: "src/app/page.tsx"
      to: "src/components/file-dropzone.tsx"
      via: "FileDropzone component rendered in documentary tab"
      pattern: "<FileDropzone"
---

<objective>
Build the file upload flow: drag-and-drop dropzone, server-side upload route with text parsing, and content preview display. After this plan, a user can upload a .txt transcript and see its parsed content with metadata.

Purpose: Deliver CORE-02 (file upload), CORE-03 (content preview), and PARSE-01 (txt parsing) as a working upload-to-preview flow.
Output: Upload API route, FileDropzone component, ContentPreview component, wired into the main page.
</objective>

<execution_context>
@C:/Users/bplan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-vertical-slice/01-RESEARCH.md
@.planning/phases/01-vertical-slice/01-UI-SPEC.md
@.planning/phases/01-vertical-slice/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/lib/parsers/txt-parser.ts:
```typescript
export interface ParseResult {
  text: string;
  metadata: {
    wordCount: number;
    lineCount: number;
    format: string;
    filename?: string;
    size?: number;
  };
}
export function parseTxt(content: string): ParseResult;
```

From src/lib/parsers/registry.ts:
```typescript
export function parseFile(content: string, filename: string): ParseResult;
```

From src/lib/types/project-types.ts:
```typescript
export interface ProjectTypeConfig {
  id: string;
  label: string;
  description: string;
  icon: string;
  acceptedExtensions: string[];
  acceptedMimeTypes: string[];
  fileTypeLabel: string;
}
export const PROJECT_TYPES: Record<string, ProjectTypeConfig>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create upload API route with file validation and text parsing</name>
  <files>
    src/app/api/upload/route.ts,
    src/app/api/upload/__tests__/route.test.ts
  </files>
  <read_first>
    src/lib/parsers/registry.ts (parseFile signature and behavior)
    src/lib/parsers/txt-parser.ts (ParseResult interface — import from here)
    .planning/phases/01-vertical-slice/01-RESEARCH.md (Pattern 3: File Upload via Route Handler — exact implementation pattern)
  </read_first>
  <behavior>
    route.test.ts:
    - POST with a .txt file in FormData returns 200 with JSON containing `text` (string) and `metadata` (object with wordCount, lineCount, format, filename, size)
    - POST with a .pdf file in FormData returns 400 with JSON containing `error: "Only .txt files are supported. Please upload a plain text transcript."`
    - POST with no file in FormData returns 400 with JSON containing `error: "No file provided"`
    - POST with a .txt file exceeding 10MB returns 400 with JSON containing `error: "File exceeds 10MB limit. Please upload a smaller file."`
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    **src/app/api/upload/__tests__/route.test.ts:**
    Test the POST handler by constructing FormData with File objects. Note: In the test environment, use the Node.js global `File` and `FormData` (available in Node 20+) or mock `NextRequest`. Import the POST handler from the route module.

    To test, create a helper that builds a Request with FormData:
    ```typescript
    function createUploadRequest(filename: string, content: string): Request {
      const file = new File([content], filename, { type: 'text/plain' });
      const formData = new FormData();
      formData.append('file', file);
      return new Request('http://localhost/api/upload', { method: 'POST', body: formData });
    }
    ```

    **src/app/api/upload/route.ts:**
    Export an async `POST` function that:
    1. Reads FormData from the request: `const formData = await req.formData()`
    2. Gets the file: `const file = formData.get('file') as File`
    3. If no file: return `NextResponse.json({ error: 'No file provided' }, { status: 400 })`
    4. If file size > 10 * 1024 * 1024: return `NextResponse.json({ error: 'File exceeds 10MB limit. Please upload a smaller file.' }, { status: 400 })`
    5. If file name does not end with '.txt': return `NextResponse.json({ error: 'Only .txt files are supported. Please upload a plain text transcript.' }, { status: 400 })`
    6. Read text: `const content = await file.text()`
    7. Parse: `const result = parseFile(content, file.name)` — import from `@/lib/parsers/registry`
    8. Add file size to metadata: `result.metadata.size = file.size`
    9. Return `NextResponse.json(result)`

    Import `NextResponse` from `next/server`. Import `parseFile` from `@/lib/parsers/registry`.
  </action>
  <verify>
    <automated>cd "F:/AI/GSD/FilmIntern" && npx vitest run src/app/api/upload --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/upload/route.ts` contains `export async function POST`
    - `src/app/api/upload/route.ts` contains `parseFile` import from `@/lib/parsers/registry`
    - `src/app/api/upload/route.ts` contains `'No file provided'` and `'Only .txt files are supported'` and `'File exceeds 10MB limit'`
    - `src/app/api/upload/__tests__/route.test.ts` exists with at least 3 test cases
    - All upload route tests pass
  </acceptance_criteria>
  <done>Upload API route accepts .txt FormData uploads, returns parsed text with metadata, rejects invalid files with specific error messages. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build FileDropzone and ContentPreview components, wire into main page</name>
  <files>
    src/components/file-dropzone.tsx,
    src/components/content-preview.tsx,
    src/app/page.tsx
  </files>
  <read_first>
    src/app/page.tsx (current page structure from Plan 01 — project type tabs setup)
    src/components/project-type-tabs.tsx (how tabs render children — need to place upload flow inside documentary tab)
    src/lib/types/project-types.ts (PROJECT_TYPES — for acceptedExtensions, fileTypeLabel)
    .planning/phases/01-vertical-slice/01-UI-SPEC.md (File Dropzone specs — size, states, border styles; Content Preview specs — max height, metadata bar, monospaced text; Upload Flow interaction states; Copywriting Contract — exact copy for dropzone, errors)
    .planning/phases/01-vertical-slice/01-RESEARCH.md (Pattern 3: File Dropzone Component — react-dropzone usage)
  </read_first>
  <action>
    **src/components/file-dropzone.tsx:**
    'use client' component. Props: `onFileUploaded: (data: { text: string; metadata: ParseResult['metadata'] }) => void`. Internal state: `uploading` (boolean), `error` (string | null).

    Use `react-dropzone` with `useDropzone`:
    - `accept: { 'text/plain': ['.txt'] }`
    - `maxFiles: 1`
    - `maxSize: 10 * 1024 * 1024` (10MB)

    On file drop accepted:
    1. Set `uploading = true`, `error = null`
    2. Create FormData, append file as 'file'
    3. `fetch('/api/upload', { method: 'POST', body: formData })`
    4. If response ok: parse JSON, call `onFileUploaded(data)`, set `uploading = false`
    5. If response not ok: parse JSON for error message, set `error = data.error`, set `uploading = false`

    On file rejected (wrong type from react-dropzone):
    - Set error to "Only .txt files are supported. Please upload a plain text transcript."
    - Clear error after 3 seconds with setTimeout

    Visual states (from UI-SPEC):
    - Default: shadcn Card, `border-2 border-dashed border-muted-foreground/25`, min-h-[200px], centered content. Upload icon (Lucide `Upload`, 40px, text-muted-foreground). Heading: "Drag & drop your transcript, or click to browse". Subtext: "Accepts .txt files up to 10MB".
    - Drag active: `border-primary bg-primary/5`, heading changes to "Drop your transcript here"
    - Uploading: Skeleton pulse animation on icon, heading "Uploading..."
    - Error: `border-destructive`, error message in text-destructive below the dropzone area
    - After successful upload: dropzone collapses to a compact file info bar showing filename, file size (human-readable), word count. Use Badge components for metadata.

    **src/components/content-preview.tsx:**
    'use client' component. Props: `text: string`, `metadata: { wordCount: number; lineCount: number; format: string; filename?: string; size?: number }`.

    Layout (from UI-SPEC):
    - shadcn Card with scrollable body
    - Metadata bar at top: Badge components showing "{wordCount} words" and "{lineCount} lines"
    - Transcript text area below: bg-muted, rounded-md, p-4, max-h-[400px] overflow-y-auto, font-mono, text-sm (14px), whitespace-pre-wrap
    - The text content is displayed as-is (plain text, preserving line breaks)

    **src/app/page.tsx:**
    Update the page to manage upload state and render the flow. Make this a 'use client' component (or extract a client wrapper).

    State:
    - `uploadData: { text: string; metadata: {...} } | null` — starts null
    - `analysisReady: boolean` — true after upload

    Inside the documentary tab content:
    1. Always show `<FileDropzone onFileUploaded={setUploadData} />`
    2. When uploadData is not null, show `<ContentPreview text={uploadData.text} metadata={uploadData.metadata} />`
    3. When uploadData is not null, show a "Run Analysis" button (shadcn Button, accent/primary variant). This button will be wired to the analysis API in Plan 03 — for now it can be present but non-functional (or show a toast/alert "Analysis coming soon").

    The page flow is vertical: dropzone at top (collapses to file info after upload), preview below, analysis button below preview.
  </action>
  <verify>
    <automated>cd "F:/AI/GSD/FilmIntern" && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/file-dropzone.tsx` contains `useDropzone` import from `react-dropzone`
    - `src/components/file-dropzone.tsx` contains `'text/plain': ['.txt']` (accept config)
    - `src/components/file-dropzone.tsx` contains `fetch('/api/upload'` (upload call)
    - `src/components/file-dropzone.tsx` contains `Drag & drop your transcript` (copy from UI-SPEC)
    - `src/components/content-preview.tsx` contains `font-mono` and `max-h-[400px]` and `overflow-y-auto`
    - `src/components/content-preview.tsx` contains `wordCount` and `lineCount` in Badge rendering
    - `src/app/page.tsx` contains `FileDropzone` and `ContentPreview` imports
    - `src/app/page.tsx` contains `Run Analysis` button text
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>User can drag-and-drop or browse to upload a .txt file, see upload progress, view parsed content preview with word/line count metadata, and see a "Run Analysis" button. Invalid files show error messages matching the copywriting contract. Build passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass (including upload route tests)
- `npm run build` — succeeds
- Dev server: can upload a .txt file and see content preview with metadata
</verification>

<success_criteria>
- Upload route accepts .txt files via FormData, rejects non-.txt with correct error messages
- FileDropzone handles drag-and-drop, click-to-browse, upload progress, and error states
- ContentPreview shows transcript text in monospaced font with word count and line count badges
- "Run Analysis" button appears after upload (wired in Plan 03)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-vertical-slice/01-02-SUMMARY.md`
</output>
