---
phase: 06-card-based-analysis-workspaces
plan: 04
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/components/workspaces/corporate-workspace.tsx
  - src/components/workspaces/tv-workspace.tsx
  - src/components/workspaces/short-form-workspace.tsx
  - src/components/workspaces/__tests__/corporate-workspace.test.tsx
  - src/components/workspaces/__tests__/tv-workspace.test.tsx
  - src/components/workspaces/__tests__/short-form-workspace.test.tsx
autonomous: true
requirements: [WORK-03, WORK-04, WORK-05]

must_haves:
  truths:
    - "Corporate workspace renders 6 messaging-specific cards"
    - "TV workspace renders 6 episode/series cards"
    - "Short-form workspace renders 6 pacing/messaging cards"
    - "All 3 workspaces have correct header titles"
    - "All handle streaming skeletons and missing optional fields"
  artifacts:
    - path: "src/components/workspaces/corporate-workspace.tsx"
      provides: "6-card corporate workspace"
      exports: ["CorporateWorkspace"]
    - path: "src/components/workspaces/tv-workspace.tsx"
      provides: "6-card TV workspace"
      exports: ["TvWorkspace"]
    - path: "src/components/workspaces/short-form-workspace.tsx"
      provides: "6-card short-form workspace"
      exports: ["ShortFormWorkspace"]
  key_links:
    - from: "src/components/workspaces/corporate-workspace.tsx"
      to: "src/lib/ai/schemas/corporate.ts"
      via: "import type { CorporateAnalysis }"
      pattern: "import.*CorporateAnalysis.*from.*schemas/corporate"
    - from: "src/components/workspaces/tv-workspace.tsx"
      to: "src/lib/ai/schemas/tv-episodic.ts"
      via: "import type { TvEpisodicAnalysis }"
      pattern: "import.*TvEpisodicAnalysis.*from.*schemas/tv-episodic"
    - from: "src/components/workspaces/short-form-workspace.tsx"
      to: "src/lib/ai/schemas/short-form.ts"
      via: "import type { ShortFormAnalysis }"
      pattern: "import.*ShortFormAnalysis.*from.*schemas/short-form"
---

<objective>
Build the Corporate (Media Prep), TV/Episodic (Series Room), and Short-Form (Impact Lab) workspace components.

Purpose: Implement WORK-03, WORK-04, WORK-05 -- the remaining 3 workspace views. These follow the same architectural pattern as Narrative/Documentary from Plan 03 but with simpler card content.
Output: 3 workspace components + 3 updated test files
</objective>

<execution_context>
@C:/Users/bplan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-card-based-analysis-workspaces/06-CONTEXT.md
@.planning/phases/06-card-based-analysis-workspaces/06-UI-SPEC.md
@.planning/phases/06-card-based-analysis-workspaces/06-RESEARCH.md
@.planning/phases/06-card-based-analysis-workspaces/06-01-SUMMARY.md

<interfaces>
<!-- Shared workspace components from Plan 01 (same as Plan 03) -->
EvaluationCard({ title, ready, children, className })
WorkspaceHeader({ title, projectType, score })
WorkspaceGrid({ children })
StreamingStatusBar({ currentSection })
EffectivenessBadge({ value })
ShowMoreToggle({ totalCount, visibleCount, expanded, onToggle })

<!-- Corporate schema (after Plan 02) -->
From src/lib/ai/schemas/corporate.ts:
  summary, soundbites: [{ quote, speaker, context, category, usability }],
  messagingThemes: [{ theme, description, evidence, consistency }],
  speakerEffectiveness: [{ speaker, strengths, areasForImprovement, quotability, onMessageScore }],
  editorialNotes: { recommendedNarrative, messagingGaps, suggestedCuts },
  spokespersonAssessment?: { overallReadiness, summary, topPerformer? },
  audienceAlignment?: { targetAudience, alignmentRating, assessment, suggestions },
  messageConsistency?: { consistencyRating, assessment, keyConflicts? },
  overallScore?, overallSummary?

<!-- TV schema (after Plan 02) -->
From src/lib/ai/schemas/tv-episodic.ts:
  episodeAnalysis: { coldOpen, storyStrands, characterIntroductions, episodeArc },
  seriesAnalysis: { premiseLongevity, serializedHooks, episodicVsSerial, seasonArcPotential },
  toneAndVoice?: { tone, voiceConsistency, assessment, comparisons? },
  pilotEffectiveness?: { worldBuilding, characterEstablishment, hookStrength, assessment },
  franchisePotential?: { potential, assessment, opportunities },
  overallScore?, overallSummary?

<!-- Short-form schema (after Plan 02) -->
From src/lib/ai/schemas/short-form.ts:
  summary, hookStrength: { opening, hookRating, timeToHook, suggestions },
  pacing: { overall, assessment, deadSpots, recommendations },
  messagingClarity: { primaryMessage, clarity, messageRetention, improvements },
  ctaEffectiveness: { hasCta, ctaText?, placement, urgency, suggestions },
  emotionalRationalBalance: { balance, assessment, emotionalMoments, rationalElements },
  audienceFit?: { primaryAudience, fitRating, assessment, suggestions },
  overallScore?, overallSummary?
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CorporateWorkspace and TvWorkspace</name>
  <files>
    src/components/workspaces/corporate-workspace.tsx
    src/components/workspaces/tv-workspace.tsx
    src/components/workspaces/__tests__/corporate-workspace.test.tsx
    src/components/workspaces/__tests__/tv-workspace.test.tsx
  </files>
  <read_first>
    src/components/workspaces/evaluation-card.tsx
    src/components/workspaces/workspace-header.tsx
    src/components/workspaces/effectiveness-badge.tsx
    src/components/workspaces/show-more-toggle.tsx
    src/components/corporate-report.tsx
    src/components/tv-report.tsx
    src/lib/ai/schemas/corporate.ts
    src/lib/ai/schemas/tv-episodic.ts
  </read_first>
  <action>
**CorporateWorkspace** (`corporate-workspace.tsx`):

Props: `{ data: Partial<CorporateAnalysis> | null; isStreaming: boolean }`
Header: `<WorkspaceHeader title="Media Prep Workspace" projectType="Corporate Interview" score={data?.overallScore} />`

Streaming detection:
```typescript
function detectCurrentSection(data: Partial<CorporateAnalysis> | null): string | null {
  if (!data) return 'Analyzing your material...';
  if (!data.soundbites) return 'Extracting key quotes...';
  if (!data.messagingThemes) return 'Identifying themes...';
  if (!data.speakerEffectiveness) return 'Assessing spokespeople...';
  if (!data.spokespersonAssessment) return 'Generating recommendations...';
  if (!data.overallScore) return 'Calculating overall assessment...';
  return null;
}
```

6 Cards:
1. **"Soundbites"**: ready=`!!(data?.soundbites?.length)`. Quote list with speaker + usability badge (usability field, not usefulness). Same pattern as documentary Key Quotes.
2. **"Key Messages"**: ready=`!!(data?.messagingThemes?.length)`. Each messaging theme: theme name (font-medium), description, evidence quotes, consistency badge.
3. **"Spokesperson Assessment"**: ready=`!!data?.spokespersonAssessment`. Overall readiness badge, summary text, top performer note. If field undefined, fallback: "Spokesperson assessment not available for this analysis".
4. **"Audience Alignment"**: ready=`!!data?.audienceAlignment`. Target audience text, alignment rating badge, assessment text, suggestions list. Fallback if undefined.
5. **"Message Consistency"**: ready=`!!data?.messageConsistency`. Consistency rating badge, assessment text, key conflicts list if present. Fallback if undefined.
6. **"Recommendations"**: ready=`!!data?.editorialNotes`. Recommended narrative text, messaging gaps as bulleted list, suggested cuts text.

**TvWorkspace** (`tv-workspace.tsx`):

Props: `{ data: Partial<TvEpisodicAnalysis> | null; isStreaming: boolean }`
Header: `<WorkspaceHeader title="Series Room Workspace" projectType="TV/Episodic" score={data?.overallScore} />`

Streaming detection:
```typescript
function detectCurrentSection(data: Partial<TvEpisodicAnalysis> | null): string | null {
  if (!data) return 'Analyzing your material...';
  if (!data.episodeAnalysis?.episodeArc) return 'Mapping story structure...';
  if (!data.seriesAnalysis) return 'Analyzing series potential...';
  if (!data.toneAndVoice) return 'Assessing tone and voice...';
  if (!data.overallScore) return 'Calculating overall assessment...';
  return null;
}
```

6 Cards:
1. **"Episode Arc"**: ready=`!!data?.episodeAnalysis?.episodeArc`. Setup/escalation/resolution text blocks, cliffhanger if present, pacing badge. Also cold open section with hook strength badge.
2. **"Series Structure"**: ready=`!!data?.seriesAnalysis`. Premise longevity badge + reasoning, episodic vs serial balance badge + assessment, season arc potential with strengths/concerns.
3. **"Character Development"**: ready=`!!(data?.episodeAnalysis?.characterIntroductions?.length)`. Each character: name, intro method, effectiveness badge. Story strands below with strand type badge + description + character list + effectiveness badge.
4. **"Tone & Voice"**: ready=`!!data?.toneAndVoice`. Tone text, voice consistency badge, assessment, comparisons as outline badges. Fallback if undefined.
5. **"Pilot Effectiveness"**: ready=`!!data?.pilotEffectiveness`. Three sub-badges: world building, character establishment, hook strength. Assessment text. Fallback if undefined.
6. **"Franchise Potential"**: ready=`!!data?.franchisePotential`. Potential badge, assessment text, opportunities list. Fallback if undefined.

**Tests** -- Replace todo stubs in both test files:
- Corporate: "renders 6 evaluation cards" (check all 6 titles), "renders Media Prep Workspace header", "shows skeletons when streaming", "handles missing optional fields"
- TV: "renders 6 evaluation cards" (check all 6 titles), "renders Series Room Workspace header", "shows skeletons when streaming", "handles missing optional fields"

Create mock data factories for CorporateAnalysis and TvEpisodicAnalysis.
  </action>
  <verify>
    <automated>cd F:/AI/GSD/FilmIntern && npx vitest run src/components/workspaces/__tests__/corporate-workspace.test.tsx src/components/workspaces/__tests__/tv-workspace.test.tsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/components/workspaces/corporate-workspace.tsx contains `export function CorporateWorkspace`
    - src/components/workspaces/corporate-workspace.tsx contains `Media Prep Workspace`
    - src/components/workspaces/corporate-workspace.tsx contains `Soundbites` and `Key Messages` and `Spokesperson Assessment` and `Audience Alignment` and `Message Consistency` and `Recommendations`
    - src/components/workspaces/tv-workspace.tsx contains `export function TvWorkspace`
    - src/components/workspaces/tv-workspace.tsx contains `Series Room Workspace`
    - src/components/workspaces/tv-workspace.tsx contains `Episode Arc` and `Series Structure` and `Character Development` and `Tone & Voice` and `Pilot Effectiveness` and `Franchise Potential`
    - Corporate tests pass: `npx vitest run src/components/workspaces/__tests__/corporate-workspace.test.tsx` exits 0
    - TV tests pass: `npx vitest run src/components/workspaces/__tests__/tv-workspace.test.tsx` exits 0
  </acceptance_criteria>
  <done>CorporateWorkspace (6 cards) and TvWorkspace (6 cards) built with correct titles, streaming detection, and optional field fallbacks. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build ShortFormWorkspace with 6 evaluation cards</name>
  <files>
    src/components/workspaces/short-form-workspace.tsx
    src/components/workspaces/__tests__/short-form-workspace.test.tsx
  </files>
  <read_first>
    src/components/workspaces/evaluation-card.tsx
    src/components/workspaces/workspace-header.tsx
    src/components/workspaces/corporate-workspace.tsx
    src/components/short-form-report.tsx
    src/lib/ai/schemas/short-form.ts
  </read_first>
  <action>
Create `src/components/workspaces/short-form-workspace.tsx`:

Props: `{ data: Partial<ShortFormAnalysis> | null; isStreaming: boolean }`
Header: `<WorkspaceHeader title="Impact Lab Workspace" projectType="Short-Form/Branded" score={data?.overallScore} />`

Streaming detection:
```typescript
function detectCurrentSection(data: Partial<ShortFormAnalysis> | null): string | null {
  if (!data) return 'Analyzing your material...';
  if (!data.hookStrength) return 'Assessing hook strength...';
  if (!data.pacing) return 'Evaluating pacing...';
  if (!data.messagingClarity) return 'Analyzing messaging clarity...';
  if (!data.ctaEffectiveness) return 'Evaluating call to action...';
  if (!data.emotionalRationalBalance) return 'Assessing emotional impact...';
  if (!data.overallScore) return 'Calculating overall assessment...';
  return null;
}
```

6 Cards:
1. **"Hook Strength"**: ready=`!!data?.hookStrength`. Opening description text, hook rating badge, time to hook text, suggestions list.
2. **"Pacing"**: ready=`!!data?.pacing`. Overall pacing badge, assessment text, dead spots list, recommendations list.
3. **"CTA Clarity"**: ready=`!!data?.ctaEffectiveness`. Has CTA indicator, CTA text if present, placement badge, urgency badge, suggestions list. Also include messaging clarity: primary message text, clarity badge, message retention text.
4. **"Brand Alignment"**: ready=`!!data?.messagingClarity`. Primary message text, clarity badge, message retention assessment, improvements list. (messagingClarity maps to Brand Alignment card per UI-SPEC which says `ctaClarity` maps to CTA Clarity and `messagingClarity` to brand messaging.)
5. **"Emotional Impact"**: ready=`!!data?.emotionalRationalBalance`. Balance badge, assessment text, emotional moments list, rational elements list.
6. **"Audience Fit"**: ready=`!!data?.audienceFit`. Primary audience text, fit rating badge, assessment text, suggestions list. Fallback if undefined: "Audience fit data not available for this analysis".

**Test file** -- Replace todo stubs:
- "renders 6 evaluation cards": check all 6 titles
- "renders Impact Lab Workspace header": check header text
- "shows skeletons when streaming": render with null data
- "handles missing audienceFit gracefully": render without audienceFit field

Create mock data factory for ShortFormAnalysis.
  </action>
  <verify>
    <automated>cd F:/AI/GSD/FilmIntern && npx vitest run src/components/workspaces/__tests__/short-form-workspace.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/workspaces/short-form-workspace.tsx contains `export function ShortFormWorkspace`
    - src/components/workspaces/short-form-workspace.tsx contains `Impact Lab Workspace`
    - src/components/workspaces/short-form-workspace.tsx contains `Hook Strength` and `Pacing` and `CTA Clarity` and `Brand Alignment` and `Emotional Impact` and `Audience Fit`
    - Short-form tests pass: `npx vitest run src/components/workspaces/__tests__/short-form-workspace.test.tsx` exits 0
    - Full suite green: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>ShortFormWorkspace (6 cards) built with correct titles, streaming detection, and optional field fallback. Tests pass. All 5 workspace components complete.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/workspaces/__tests__/ --reporter=verbose` -- all workspace tests pass
- `npx vitest run --reporter=verbose` -- full suite green
- `npx tsc --noEmit` -- no type errors
</verification>

<success_criteria>
- CorporateWorkspace renders 6 cards per WORK-03
- TvWorkspace renders 6 cards per WORK-04
- ShortFormWorkspace renders 6 cards per WORK-05
- All handle streaming and missing optional fields gracefully
- All workspace tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-card-based-analysis-workspaces/06-04-SUMMARY.md`
</output>
