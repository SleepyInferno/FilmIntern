---
phase: 04-export-and-document-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/documents/types.ts
  - src/lib/documents/availability.ts
  - src/lib/documents/report-normalization.ts
  - src/lib/documents/report-document.ts
  - src/lib/documents/__tests__/availability.test.ts
  - src/lib/documents/__tests__/report-document.test.ts
autonomous: true
requirements:
  - OUTP-02
  - OUTP-03
must_haves:
  truths:
    - "After any supported analysis completes, the user can open an export-ready report document built from one shared document record."
    - "The document actions shown for a selected project type stay consistent regardless of which analysis report type produced the result."
    - "Quotes and report sections from every analysis report type carry into the editable/exportable document state instead of being re-authored per export format."
  artifacts:
    - path: "src/lib/documents/types.ts"
      provides: "DocumentKind, OutlineMode, ExportFormat, GeneratedDocument, DocumentQuoteRef, DocumentCover"
    - path: "src/lib/documents/availability.ts"
      provides: "Project-type-to-document-kind matrix and outline mode rules"
    - path: "src/lib/documents/report-normalization.ts"
      provides: "Generic report normalization contract plus adapters for every supported analysis schema"
    - path: "src/lib/documents/report-document.ts"
      provides: "Conversion from supported analysis payloads into GeneratedDocument report records"
  key_links:
    - from: "src/lib/documents/availability.ts"
      to: "src/lib/types/project-types.ts"
      via: "project type IDs imported from PROJECT_TYPES"
      pattern: "PROJECT_TYPES"
    - from: "src/lib/documents/report-normalization.ts"
      to: "src/lib/ai/schemas"
      via: "schema-specific normalizers registered for each Phase 3 analysis output"
      pattern: "Analysis"
    - from: "src/lib/documents/report-document.ts"
      to: "src/lib/documents/report-normalization.ts"
      via: "buildReportDocument delegates section and quote mapping through the registry"
      pattern: "normalizeReport"
---

<objective>
Create the shared document contracts Phase 4 depends on so generation, editing, and export all operate on one canonical document record.

Purpose: Prevent PDF/DOCX export drift and keep project-type document rules centralized before UI and route work begins.
Output: Shared document types, availability matrix, report-to-document adapter, and unit tests that lock the contracts.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-export-and-document-generation/04-CONTEXT.md
@.planning/phases/04-export-and-document-generation/04-RESEARCH.md
@.planning/phases/04-export-and-document-generation/04-VALIDATION.md
@src/lib/types/project-types.ts
@src/lib/ai/schemas/documentary.ts
@src/lib/ai/schemas/corporate.ts
@src/lib/ai/schemas/narrative.ts
@src/lib/ai/schemas/tv-episodic.ts
@src/lib/ai/schemas/short-form.ts
@src/components/analysis-report.tsx

<interfaces>
From src/lib/types/project-types.ts:
```typescript
export interface ProjectTypeConfig {
  id: string;
  label: string;
  description: string;
  icon: string;
  acceptedExtensions: string[];
  acceptedMimeTypes: string[];
  fileTypeLabel: string;
}

export const PROJECT_TYPES: Record<string, ProjectTypeConfig>;
```

From src/lib/ai/schemas/documentary.ts:
```typescript
export type DocumentaryAnalysis = z.infer<typeof documentaryAnalysisSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 4 document dependencies and shared document contracts</name>
  <read_first>
    - package.json
    - src/lib/types/project-types.ts
    - src/lib/ai/schemas/documentary.ts
    - src/lib/ai/schemas/corporate.ts
    - src/lib/ai/schemas/narrative.ts
    - src/lib/ai/schemas/tv-episodic.ts
    - src/lib/ai/schemas/short-form.ts
    - .planning/phases/04-export-and-document-generation/04-CONTEXT.md
    - .planning/phases/04-export-and-document-generation/04-RESEARCH.md
  </read_first>
  <files>package.json, package-lock.json, src/lib/documents/types.ts, src/lib/documents/availability.ts, src/lib/documents/__tests__/availability.test.ts</files>
  <behavior>
    - Test 1: `getAvailableDocumentKinds('documentary')` returns `['report', 'outline', 'proposal']`.
    - Test 2: `getAvailableDocumentKinds('corporate')` returns `['report', 'outline', 'proposal']`.
    - Test 3: `getAvailableDocumentKinds('narrative')` and `getAvailableDocumentKinds('tv-episodic')` return `['report', 'outline', 'treatment']`.
    - Test 4: `getAvailableDocumentKinds('short-form')` returns `['report', 'outline']`.
    - Test 5: `getOutlineModes('narrative')` and `getOutlineModes('tv-episodic')` return `['beats', 'scene-by-scene']`, while documentary, corporate, and short-form return `['beats']`.
  </behavior>
  <action>Install `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/pm`, `@tiptap/static-renderer`, `docx`, and `playwright`, then run `npx playwright install chromium`. Create `src/lib/documents/types.ts` exporting these exact unions and interfaces: `type DocumentKind = 'report' | 'outline' | 'treatment' | 'proposal'`; `type OutlineMode = 'beats' | 'scene-by-scene'`; `type ExportFormat = 'pdf' | 'docx'`; `interface DocumentQuoteRef { id: string; label: string; text: string; speaker?: string; sourceSection: 'summary' | 'keyQuotes' | 'recurringThemes' | 'keyMoments' | 'editorialNotes'; }`; `interface DocumentCover { title: string; typeLabel: string; writtenBy: string; dateLabel: string; projectTypeLabel: string; }`; `interface GeneratedDocument { id: string; kind: DocumentKind; projectType: string; title: string; writtenBy: string; createdAt: string; updatedAt: string; outlineMode?: OutlineMode; cover: DocumentCover; content: Record<string, unknown>; quoteRefs: DocumentQuoteRef[]; sourceText: string; analysisSnapshot?: Record<string, unknown>; }`. Create `src/lib/documents/availability.ts` with exact exported constants for the matrix and helper functions `getAvailableDocumentKinds(projectType: string)`, `getOutlineModes(projectType: string)`, and `supportsDocumentKind(projectType: string, kind: DocumentKind)`. The matrix must hardcode: documentary=`report, outline, proposal`; corporate=`report, outline, proposal`; narrative=`report, outline, treatment`; tv-episodic=`report, outline, treatment`; short-form=`report, outline`. The module must derive valid project type IDs from `PROJECT_TYPES` and throw `Error('Unknown project type: ' + projectType)` for unknown IDs. Write `src/lib/documents/__tests__/availability.test.ts` to lock the matrix and helper behavior before implementation.</action>
  <acceptance_criteria>
    - `rg "type DocumentKind = 'report' \\| 'outline' \\| 'treatment' \\| 'proposal'" src/lib/documents/types.ts` returns one match.
    - `rg "type ExportFormat = 'pdf' \\| 'docx'" src/lib/documents/types.ts` returns one match.
    - `rg "documentary:\\s*\\['report', 'outline', 'proposal'\\]" src/lib/documents/availability.ts` returns one match.
    - `rg "short-form:\\s*\\['report', 'outline'\\]" src/lib/documents/availability.ts` returns one match.
    - `rg "Unknown project type" src/lib/documents/availability.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/documents/__tests__/availability.test.ts</automated>
  </verify>
  <done>Dependencies are installed, the canonical document contracts exist, and the project-type document availability rules are covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the generic report-normalization contract and map every analysis schema into GeneratedDocument</name>
  <read_first>
    - src/lib/ai/schemas/documentary.ts
    - src/lib/ai/schemas/corporate.ts
    - src/lib/ai/schemas/narrative.ts
    - src/lib/ai/schemas/tv-episodic.ts
    - src/lib/ai/schemas/short-form.ts
    - src/components/analysis-report.tsx
    - src/lib/documents/types.ts
    - src/lib/documents/availability.ts
    - .planning/phases/04-export-and-document-generation/04-CONTEXT.md
  </read_first>
  <files>src/lib/documents/report-normalization.ts, src/lib/documents/report-document.ts, src/lib/documents/__tests__/report-document.test.ts</files>
  <behavior>
    - Test 1: `buildReportDocument` accepts every supported Phase 3 report schema and returns a `GeneratedDocument` with `kind: 'report'`, a populated cover page, and Tiptap-compatible `content`.
    - Test 2: documentary, corporate, narrative structure, narrative coverage, tv-episodic, and short-form analyses each map through a registered normalizer instead of documentary-only branching.
    - Test 3: quote-bearing sections become `quoteRefs` with stable labels `Q1`, `Q2`, and preserve quote text and speaker across report types that expose quotes.
    - Test 4: the adapter stores the original analysis payload in `analysisSnapshot` and the uploaded text in `sourceText` for every supported report variant.
  </behavior>
  <action>Create `src/lib/documents/report-normalization.ts` exporting the generic contract layer Phase 4 depends on: `type AnalysisReportKind = 'documentary' | 'corporate' | 'narrative-structure' | 'narrative-coverage' | 'tv-episodic' | 'short-form'`; a typed `ReportNormalizer<TAnalysis>` interface; and a `reportNormalizers` registry keyed by that union. The registry must explicitly include adapters for the exact Phase 3 schema files `src/lib/ai/schemas/documentary.ts`, `src/lib/ai/schemas/corporate.ts`, `src/lib/ai/schemas/narrative.ts` (both structure and coverage report shapes), `src/lib/ai/schemas/tv-episodic.ts`, and `src/lib/ai/schemas/short-form.ts`. Update `src/lib/documents/report-document.ts` to export `buildReportDocument(input)` where `input` is a discriminated union containing `{ reportKind, projectType, analysis, sourceText, title, writtenBy, createdAt? }`. `buildReportDocument` must delegate section assembly, quote extraction, and cover labeling through `reportNormalizers[reportKind]`, then return a `GeneratedDocument` with `kind: 'report'`, ISO `createdAt`/`updatedAt`, `outlineMode: undefined`, Tiptap `content`, extracted `quoteRefs`, `sourceText`, and `analysisSnapshot`. Do not special-case documentary inside `buildReportDocument`; documentary is one adapter entry in the registry, not the default implementation. Use `typeLabel: 'Analysis Report'` for all normalized report exports unless a specific adapter provides a more precise label from the source schema.</action>
  <acceptance_criteria>
    - `rg "export function buildReportDocument" src/lib/documents/report-document.ts` returns one match.
    - `rg "AnalysisReportKind" src/lib/documents/report-normalization.ts` returns one match.
    - `rg "narrative-coverage" src/lib/documents/report-normalization.ts` returns one match.
    - `rg "tv-episodic" src/lib/documents/report-normalization.ts` returns one match.
    - `rg "reportNormalizers" src/lib/documents/report-normalization.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/documents/__tests__/report-document.test.ts</automated>
  </verify>
  <done>Every supported analysis schema can be normalized into the same GeneratedDocument report record with stable section mapping, cover metadata, editable content JSON, and preserved quote references.</done>
</task>

</tasks>

<verification>
Run the two new unit test files plus `npm test` before closing the plan so the shared contract layer is stable for downstream plans.
</verification>

<success_criteria>
Phase 4 has a single source of truth for document kinds, outline modes, export formats, and generated document state, and every supported analysis report can be normalized into that state without UI assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-document-generation/04-01-SUMMARY.md`
</output>
