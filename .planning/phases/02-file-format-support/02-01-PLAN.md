---
phase: 02-file-format-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/parsers/registry.ts
  - src/lib/parsers/__tests__/txt-parser.test.ts
  - src/app/api/upload/route.ts
  - src/app/api/upload/__tests__/route.test.ts
  - src/components/file-dropzone.tsx
  - src/lib/types/project-types.ts
  - src/lib/types/__tests__/project-types.test.ts
autonomous: true
requirements:
  - PARSE-02
  - PARSE-03
  - PARSE-04

must_haves:
  truths:
    - "Users can select and upload .txt, .pdf, .fdx, and .docx files without client-side rejection"
    - "Unsupported extensions such as .mp4 return a 400 response with the accepted format list in the error message"
    - "The upload route reads .txt as text and binary formats as Buffer input before dispatching to the parser registry"
    - "Project type configs advertise the same supported extensions and MIME types used by the upload UI"
  artifacts:
    - path: "src/app/api/upload/route.ts"
      provides: "Binary-aware upload handling and extension validation"
      contains: "file.arrayBuffer()"
    - path: "src/lib/parsers/registry.ts"
      provides: "Async parseFile dispatching by extension"
      contains: "export async function parseFile"
    - path: "src/components/file-dropzone.tsx"
      provides: "Client accept configuration for all supported upload formats"
      contains: "application/pdf"
    - path: "src/lib/types/project-types.ts"
      provides: "Per-project acceptedExtensions and acceptedMimeTypes"
      contains: "acceptedExtensions"
  key_links:
    - from: "src/app/api/upload/route.ts"
      to: "src/lib/parsers/registry.ts"
      via: "await parseFile(contentOrBuffer, file.name)"
      pattern: "await parseFile"
    - from: "src/components/file-dropzone.tsx"
      to: "src/lib/types/project-types.ts"
      via: "UI-supported extensions must match project-type config"
      pattern: "acceptedExtensions"
---

<objective>
Build the Phase 2 foundation without depending on parser implementations that do not exist yet: install parser dependencies, make the registry and upload route binary-safe, expand client acceptance for all supported file formats, and align project-type config/tests with the same extension policy.

Purpose: Plan 02 will implement parser behavior and structure-preserving preview output. This plan only establishes the infrastructure that makes those parser implementations possible.
Output: Async registry, upload route that accepts binary formats, dropzone/project-type acceptance expanded, and tests updated to verify the infrastructure behavior in a PowerShell-safe way.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-format-support/02-RESEARCH.md

<interfaces>
From src/lib/parsers/txt-parser.ts:
```typescript
export interface ParseResult {
  text: string;
  metadata: {
    wordCount: number;
    lineCount: number;
    format: string;
    filename?: string;
    size?: number;
  };
}

export function parseTxt(content: string): ParseResult;
```

From src/lib/parsers/registry.ts:
```typescript
export function parseFile(content: string, filename: string): ParseResult;
```

From src/app/api/upload/route.ts:
```typescript
export async function POST(req: Request);
```

From src/components/file-dropzone.tsx:
```typescript
accept: { 'text/plain': ['.txt'] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install parser dependencies and make upload infrastructure binary-safe</name>
  <files>
    package.json,
    src/lib/parsers/registry.ts,
    src/lib/parsers/__tests__/txt-parser.test.ts,
    src/app/api/upload/route.ts,
    src/app/api/upload/__tests__/route.test.ts
  </files>
  <read_first>
    package.json,
    src/lib/parsers/registry.ts,
    src/lib/parsers/txt-parser.ts,
    src/lib/parsers/__tests__/txt-parser.test.ts,
    src/app/api/upload/route.ts,
    src/app/api/upload/__tests__/route.test.ts,
    .planning/phases/02-file-format-support/02-RESEARCH.md
  </read_first>
  <action>
1. Install the parser libraries exactly as researched:
   ```bash
   npm install pdf-parse fast-xml-parser mammoth
   ```

2. Update `src/lib/parsers/registry.ts` so `parseFile` becomes:
   ```typescript
   export async function parseFile(content: Buffer | string, filename: string): Promise<ParseResult>
   ```
   Keep `.txt` parsing functional by converting Buffer input to UTF-8 text. Add `.pdf`, `.fdx`, and `.docx` cases that throw `Parser for <ext> not yet implemented` for now. Do not import or reference parser modules that Plan 02 has not created yet.

3. Update `src/app/api/upload/route.ts` to:
   - define `ALLOWED_EXTENSIONS = ['.txt', '.pdf', '.fdx', '.docx']`
   - reject unsupported extensions with `Unsupported file format: ${ext}. Accepted formats: .txt, .pdf, .fdx, .docx`
   - call `file.text()` for `.txt`
   - call `Buffer.from(await file.arrayBuffer())` for `.pdf`, `.fdx`, and `.docx`
   - await `parseFile(...)` in all cases

4. Update `src/app/api/upload/__tests__/route.test.ts` so the route tests cover:
   - `.txt` upload still succeeds
   - `.mp4` returns a 400 with `Unsupported file format: .mp4`
   - `.pdf` reaches the registry and returns the placeholder `not yet implemented` parser error

5. Update `src/lib/parsers/__tests__/txt-parser.test.ts` with an async registry wrapper section that verifies:
   - `await parseFile('Hello world', 'test.txt')` returns `metadata.format === 'txt'`
   - `await parseFile(Buffer.from('Buffer content'), 'test.txt')` returns `metadata.filename === 'test.txt'`
   - `await parseFile('content', 'file.xyz')` rejects with `Unsupported file format: .xyz`
  </action>
  <verify>
    <automated>npx vitest run src/lib/parsers/__tests__/txt-parser.test.ts src/app/api/upload/__tests__/route.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"pdf-parse"`, `"fast-xml-parser"`, and `"mammoth"` in dependencies
    - `src/lib/parsers/registry.ts` contains `async function parseFile(content: Buffer | string`
    - `src/lib/parsers/registry.ts` contains `case '.pdf':` and the string `not yet implemented`
    - `src/app/api/upload/route.ts` contains `ALLOWED_EXTENSIONS = ['.txt', '.pdf', '.fdx', '.docx']`
    - `src/app/api/upload/route.ts` contains `file.arrayBuffer()`
    - `src/app/api/upload/route.ts` contains `await parseFile(`
    - `src/app/api/upload/__tests__/route.test.ts` contains `.mp4`
    - `src/lib/parsers/__tests__/txt-parser.test.ts` contains `await parseFile(`
    - `npx vitest run src/lib/parsers/__tests__/txt-parser.test.ts src/app/api/upload/__tests__/route.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>Dependencies are installed, the parser registry is async and binary-aware, the upload route accepts all four supported formats, unsupported extensions fail with a descriptive 400, and the infrastructure tests pass without importing parser modules that do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Expand client accept rules and project-type support for the new formats</name>
  <files>
    src/components/file-dropzone.tsx,
    src/lib/types/project-types.ts,
    src/lib/types/__tests__/project-types.test.ts
  </files>
  <read_first>
    src/components/file-dropzone.tsx,
    src/lib/types/project-types.ts,
    src/lib/types/__tests__/project-types.test.ts,
    .planning/ROADMAP.md
  </read_first>
  <action>
1. Update `src/components/file-dropzone.tsx` so the `accept` configuration is exactly:
   ```typescript
   {
     'text/plain': ['.txt'],
     'application/pdf': ['.pdf'],
     'application/xml': ['.fdx'],
     'text/xml': ['.fdx'],
     'application/vnd.openxmlformats-officedocument.wordprocessingml.document': ['.docx'],
   }
   ```
   Change the rejection copy to `Unsupported file type. Accepted formats: .txt, .pdf, .fdx, .docx` and the helper text to `Accepts .txt, .pdf, .fdx, .docx files up to 10MB`.

2. Update `src/lib/types/project-types.ts` so project type support matches expected user workflows:
   - `documentary`: `.txt`, `.pdf`, `.docx`
   - `corporate`: `.txt`, `.pdf`, `.docx`
   - `narrative`: `.pdf`, `.fdx`, `.docx`
   - `tv-episodic`: `.pdf`, `.fdx`, `.docx`
   - `short-form`: `.txt`, `.pdf`, `.fdx`, `.docx`

3. Update `src/lib/types/__tests__/project-types.test.ts` so tests assert the new acceptedExtensions arrays exactly, including a narrative test that expects `['.pdf', '.fdx', '.docx']`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/types/__tests__/project-types.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/file-dropzone.tsx` contains `'application/pdf': ['.pdf']`
    - `src/components/file-dropzone.tsx` contains `Accepts .txt, .pdf, .fdx, .docx files up to 10MB`
    - `src/components/file-dropzone.tsx` contains `Unsupported file type. Accepted formats: .txt, .pdf, .fdx, .docx`
    - `src/lib/types/project-types.ts` documentary `acceptedExtensions` contains `.pdf` and `.docx`
    - `src/lib/types/project-types.ts` narrative `acceptedExtensions` equals `.pdf`, `.fdx`, `.docx`
    - `src/lib/types/__tests__/project-types.test.ts` contains `'.pdf', '.fdx', '.docx'`
    - `npx vitest run src/lib/types/__tests__/project-types.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>Client upload affordances and project-type configuration advertise the same supported file formats, so users can select valid inputs for each project type before parser implementation lands in Plan 02.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/parsers/__tests__/txt-parser.test.ts src/app/api/upload/__tests__/route.test.ts src/lib/types/__tests__/project-types.test.ts --reporter=verbose`
- `npx tsc --noEmit`
- `Select-String -Path src/app/api/upload/route.ts -Pattern "ALLOWED_EXTENSIONS"`
- `Select-String -Path src/components/file-dropzone.tsx -Pattern "application/pdf"`
</verification>

<success_criteria>
- Users can attempt `.txt`, `.pdf`, `.fdx`, and `.docx` uploads without client-side rejection
- Upload route handles binary formats with `arrayBuffer()` and text with `text()`
- Unsupported extensions return a descriptive 400 that lists accepted formats
- Project type configuration matches the upload UI's supported extensions
- Infrastructure tests pass without depending on parser modules that are created later
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-format-support/02-01-SUMMARY.md`
</output>
