---
phase: 15-adjustments-revision-page-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/app-sidebar.tsx
  - src/components/app-topnav.tsx
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "Browser tab title reads 'Film Intern' not 'Nano Banana'"
    - "Top navigation bar displays 'Film Intern' (with space) not 'FilmIntern'"
    - "Sidebar brand text reads 'Film Intern' when expanded and 'FI' when collapsed"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Metadata title"
      contains: 'title: "Film Intern"'
    - path: "src/components/app-sidebar.tsx"
      provides: "Sidebar brand text"
      contains: "Film Intern"
    - path: "src/components/app-topnav.tsx"
      provides: "Top nav brand text"
      contains: "Film Intern"
  key_links: []
---

<objective>
Rename all instances of "Nano Banana" (and the no-space "FilmIntern" variant) to "Film Intern" across the application.

Purpose: BRAND-01 requires the app identity to be "Film Intern" everywhere -- page titles, headers, navigation, metadata.
Output: Three files updated with correct branding.
</objective>

<execution_context>
@C:/Users/bplan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-adjustments-revision-page-branding/15-RESEARCH.md
@.planning/phases/15-adjustments-revision-page-branding/15-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update branding in layout metadata and sidebar</name>
  <files>src/app/layout.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - src/app/layout.tsx (current metadata title on line 16)
    - src/components/app-sidebar.tsx (brand text around lines 117-121)
  </read_first>
  <action>
In `src/app/layout.tsx`, change line 16 from:
```
title: "Nano Banana",
```
to:
```
title: "Film Intern",
```

In `src/components/app-sidebar.tsx`, change line 117 from:
```
Nano Banana
```
to:
```
Film Intern
```

In `src/components/app-sidebar.tsx`, change lines 119-120 the collapsed abbreviation from:
```
NB
```
to:
```
FI
```
  </action>
  <verify>
    <automated>cd F:/ai/gsd/FilmIntern && grep -n "Nano Banana" src/app/layout.tsx src/components/app-sidebar.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Nano Banana" src/app/layout.tsx` returns zero matches
    - `grep 'title: "Film Intern"' src/app/layout.tsx` returns exactly one match
    - `grep "Nano Banana" src/components/app-sidebar.tsx` returns zero matches
    - `grep "Film Intern" src/components/app-sidebar.tsx` returns exactly one match
    - `grep ">FI<" src/components/app-sidebar.tsx` returns one match (collapsed abbreviation)
    - `grep ">NB<" src/components/app-sidebar.tsx` returns zero matches
  </acceptance_criteria>
  <done>Layout metadata title is "Film Intern". Sidebar displays "Film Intern" expanded and "FI" collapsed. No "Nano Banana" or "NB" references remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Update branding in top navigation</name>
  <files>src/components/app-topnav.tsx</files>
  <read_first>
    - src/components/app-topnav.tsx (brand text on line 33, currently "FilmIntern" without space)
  </read_first>
  <action>
In `src/components/app-topnav.tsx`, change line 33 from:
```
FilmIntern
```
to:
```
Film Intern
```

This adds the space to match the BRAND-01 requirement ("Film Intern" -- two words, capital F capital I).
  </action>
  <verify>
    <automated>cd F:/ai/gsd/FilmIntern && grep "Film Intern" src/components/app-topnav.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Film Intern" src/components/app-topnav.tsx` returns exactly one match
    - `grep "FilmIntern" src/components/app-topnav.tsx` returns zero matches (the no-space variant is gone)
  </acceptance_criteria>
  <done>Top navigation displays "Film Intern" (with space). All branding across the app now consistently reads "Film Intern".</done>
</task>

</tasks>

<verification>
Run a full project grep to confirm no "Nano Banana" references remain in source code:
```bash
grep -r "Nano Banana" src/ --include="*.tsx" --include="*.ts" | wc -l
```
Expected: 0

Build passes:
```bash
npm run build
```
</verification>

<success_criteria>
- Zero instances of "Nano Banana" in any .ts/.tsx source file
- Zero instances of "NB" as brand abbreviation in sidebar
- "Film Intern" appears in layout.tsx metadata, app-sidebar.tsx, and app-topnav.tsx
- `npm run build` passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-adjustments-revision-page-branding/15-01-SUMMARY.md`
</output>
