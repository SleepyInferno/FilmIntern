---
phase: 06-card-based-analysis-workspaces
task: 2
total_tasks: 2
status: in_progress
last_updated: "2026-03-18T16:58:14.258Z"
---

<current_state>
Paused for machine reboot. Still at the human verification checkpoint for plan 06-05 (final plan of phase 6). Task 1 is committed (c878c59) — all 5 workspaces wired into page.tsx and document-workspace.tsx.

This session also implemented a prompt quality upgrade (out of band, uncommitted):
- Track 1: Hard rules + quality formula added to all 5 project type prompts
- Track 2: Narrative schema upgraded with `storyAngle`, `verdict`, `roleFunction`, `innerConflict`; narrative workspace updated to render them

The analyze button is broken (1-2 second load then stops) — root cause unconfirmed. The npm dev server was also having lock file issues (stale .next/dev/lock) requiring a reboot to clear. After reboot, run `npm run dev` and check terminal for `Analysis streaming error:` log.
</current_state>

<completed_work>

**Phase 06-05 Task 1 (committed c878c59):**
- All 5 workspace types wired into page.tsx (streaming phase) and document-workspace.tsx (Report tab)
- WorkspaceForType helper routes by projectType
- DocumentWorkspace receives analysisData + workspaceProjectType props

**Prompt/schema upgrade (uncommitted — out of band):**
- `src/lib/ai/prompts/narrative.ts` — full replacement: persona from analysis_prompts/01_system_prompt.txt, character function labels, story angle instruction, quality formula, verdict section
- `src/lib/ai/prompts/documentary.ts` — hard rules added (no generic praise, no invented details, no repeated notes, function > description)
- `src/lib/ai/prompts/corporate.ts` — same hard rules
- `src/lib/ai/prompts/short-form.ts` — same hard rules
- `src/lib/ai/prompts/tv-episodic.ts` — same hard rules
- `src/lib/ai/schemas/narrative.ts` — `storyAngle: string` (required), `verdict` object (verbal: concept/execution/dialogue/characters/marketability/overall, optional), `roleFunction` + `innerConflict` on characters (optional)
- `src/components/workspaces/narrative-workspace.tsx` — storyAngle callout above WorkspaceGrid, roleFunction badge on characters, innerConflict italic quote, verdict grid panel below cards
- Test fixtures updated in narrative.test.ts, narrative-workspace.test.tsx, report-document.test.ts
- All new code passes; 12 pre-existing failures unchanged
</completed_work>

<remaining_work>

**Analyze button bug (fix first):**
- Symptom: 1-2 second load then stops, no analysis, no visible error shown to user
- Check `npm run dev` terminal for `Analysis streaming error:` log after clicking Analyze
- Settings: provider=openai, model=gpt-5.4 — user confirmed previously working
- Likely a silent streaming error from OpenAI (caught by onError handler, not surfaced to client)

**06-05 Task 2 — human verification (blocks phase close):**
- Run `npm run dev`, open http://localhost:3000
- Test Narrative Film: workspace cards appear during and after streaming, story angle callout visible, verdict panel at bottom
- Test Documentary: 6 cards render
- Test Export (PDF/DOCX) and Generate Outline/Treatment still work
- Type "approved" or describe issues

**After approval:**
- Create 06-05-SUMMARY.md
- Update STATE.md + ROADMAP.md
- Run gsd-verifier (VERIFICATION.md)
- Mark phase 06 complete

**Commit prompt/schema changes separately** (they're a clean logical unit, not part of phase 06 task)
</remaining_work>

<decisions_made>

- analysis_prompts/ pack is 2-pass; Pass 2 (prose rendering) is redundant since app uses card workspaces — only Pass 1 persona + schema concepts integrated
- `verdict` and character fields (`roleFunction`, `innerConflict`) added as optional to preserve backward compat
- `overallScore` (numeric) kept alongside new `verdict` (verbal) — not removed
- Character badge shows `roleFunction` when present, falls back to `role` enum
- Story angle: bordered left-bar callout between streaming status and WorkspaceGrid
- Verdict: full-width grid panel below WorkspaceGrid (outside cards)
- Wave 1 decisions from previous session still apply (EvaluationCard ready={true} pattern, etc.)
</decisions_made>

<blockers>

- **Analyze button not working**: User cannot verify phase 06 until analysis runs. The error is silent on the client — check server terminal for `Analysis streaming error:` after clicking Analyze.
</blockers>

<context>
Phase 6 is complete in terms of code — all 5 workspaces built (06-01 through 06-04) and wired (06-05 Task 1). Only human verification remains to close the phase.

The prompt/schema work came from the user's analysis_prompts/ directory — a high-quality screenplay analysis pack. Key concepts integrated: story angle (what the story is really about beneath the plot), verbal verdict ratings, character function labels (emotional anchor, truth catalyst, mirror character, etc.), and the quality formula (theme > plot, function > description, subtext > summary, emotional pacing > act labels).

The analyze bug appeared mid-session. The settings have provider=openai/model=gpt-5.4 which the user confirmed was previously working. Most likely a transient API issue or silent model error — checking the terminal log is the fastest path to diagnosis.
</context>

<next_action>
1. Run `npm run dev`, click Analyze, check terminal for `Analysis streaming error:` to diagnose the broken analyze button
2. Once analysis works: run human verification for 06-05 Task 2
3. Type "approved" to complete phase 06
4. Commit the prompt/schema changes (separate commit from phase 06 work)
5. Resume orchestrator to create SUMMARY.md and run phase verification
</next_action>
