---
phase: 06-card-based-analysis-workspaces
task: ad-hoc-ux-iteration
total_tasks: ad-hoc-ux-iteration
status: in_progress
last_updated: "2026-03-18T18:57:30.607Z"
---

<current_state>
Extensive UX iteration on project creation flow — all code changes complete and type-clean but UNCOMMITTED. Dev server was running (localhost:3002 last seen). User still testing sidebar/new-project UX.

Phase 06-05 human verification still pending (plan has no SUMMARY). The ad-hoc work this session is a full redesign of the project creation/persistence UX.
</current_state>

<completed_work>

**Phase 06-05 Task 1 (committed c878c59):**
- All 5 workspace types wired into page.tsx and document-workspace.tsx

**Prompt/schema upgrade (committed 77ff7d4):**
- Narrative schema nullable fields, verdict/storyAngle/roleFunction additions
- Doc gen prompts rewritten with professional structure

**This session — full UX redesign of project creation flow (ALL UNCOMMITTED, ~36 files changed):**

1. `src/contexts/workspace-context.tsx`
   - Replaced `isSaved`/`setIsSaved` with `isNewProjectMode`/`setIsNewProjectMode`
   - `resetWorkspace()` sets `isNewProjectMode = false`
   - `loadProject()` sets `isNewProjectMode = false`
   - `title` initial value changed from 'Untitled' to ''

2. `src/app/page.tsx`
   - Removed manual Save button and `isSaved` logic entirely
   - Restored auto-save after analysis completes
   - `showCreationUI` flag: true when `isNewProjectMode` OR `uploadData && !reportDocument && !isAnalyzing`
   - `showEmptyState` flag: true when not creating and no active analysis
   - Empty state: centered Film icon + "No project open" + "click + to start"
   - New project form: "New Project" header + Cancel button + Project Name input + type dropdown + dropzone
   - `handleFileUploaded` now creates project stub in DB immediately on file drop (sidebar entry before analysis runs)
   - `handleCancelNew()` clears upload + sets `isNewProjectMode = false`
   - `ensureProject` uses "New Project" as final fallback title
   - Auto-fill title from filename on drop

3. `src/components/projects-sidebar.tsx`
   - Sidebar re-fetch split into two effects: mount-only + only when `currentProjectId` goes from null to non-null (prevents flicker on workspace reset)
   - `handleNew`: defensive save of unsaved data before reset; saves project stub if `!currentProjectId && (uploadData || analysisData)`
   - "+" button disabled when `isNewProjectMode` (can't start another project while creating one)
   - "New project…" spinning indicator at top of sidebar list when `isNewProjectMode`
   - Imports `uploadData` from context for defensive save logic

**Previous session ad-hoc work (also uncommitted):**
- Short-Form/Branded removed entirely (7 files deleted)
- `better-sqlite3` SQLite DB — `src/lib/db.ts`
- Project API routes: `src/app/api/projects/route.ts`, `src/app/api/projects/[id]/route.ts`
- Sidebar, layout, topnav changes
- `dev.db` already in `.gitignore`
- Tests: 9 failing (pre-existing), 188 passing

</completed_work>

<remaining_work>

**1. Human verification of project flow:**
- Restart dev server: `rm -f .next/dev/lock && npm run dev`
- Open http://localhost:3000
- Drop a file → project stub appears in sidebar immediately
- Run analysis → project saved with full data
- Click "+" → shows New Project form, "+" is greyed out, old project still in sidebar
- Cancel → returns to empty state, click old project to reopen
- Verify: clicking a sidebar project while in new project mode opens it correctly

**2. Commit all uncommitted changes (2 commits):**
- Commit 1: schema/workspace/prompt fixes (narrative .nullable(), doc gen prompts)
- Commit 2: project persistence + UX redesign (this session)

**3. Phase 06 close:**
- Create `06-05-SUMMARY.md`
- Create `VERIFICATION.md`
- Update `STATE.md` + `ROADMAP.md`
- Mark phase 06 complete

</remaining_work>

<decisions_made>

- **`isNewProjectMode` in context** — sidebar needs to read + set it; only works via shared context
- **Project stub created on file drop** — ensures sidebar entry exists even if user clicks "+" before running analysis
- **"+" disabled while in new project mode** — prevents accidental reset of in-progress creation
- **Sidebar re-fetch split into two effects** — prevents project list from disappearing when workspace resets
- **Auto-save restored** — manual save was superseded by user request for auto-save-on-analysis-complete
- **Title defaults to ''** — auto-filled from filename on drop; "New Project" fallback if no filename
- **better-sqlite3 over Prisma** — Prisma 7 dropped direct DB connections; better-sqlite3 simpler for local tool
- **Sidebar in root layout** — always visible across all pages

</decisions_made>

<blockers>

None — all code is type-clean. UX still being validated by user.

</blockers>

<context>
This session was primarily UX iteration around a confusing flow where clicking "+" collapsed an active project with no way to re-open it. Multiple iterations were needed to arrive at the current design:
- Upload → immediate DB stub → always in sidebar
- "+" disabled while creating
- Sidebar list stays stable on reset (no flicker)
- Cancel button to exit creation mode

The 06-05-PLAN.md task (wiring workspaces) was done in a prior session. Remaining work is human verification of the full flow + close-out.
</context>

<next_action>
1. `rm -f .next/dev/lock && npm run dev` → verify new project flow at http://localhost:3000
2. If approved, commit in 2 commits
3. Type "approved" to close phase 06 (SUMMARY + VERIFICATION)
4. Then plan/execute Phase 7: Library Persistence
</next_action>
