---
phase: 01-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/lib/types/project-types.ts
  - src/lib/ai/schemas/documentary.ts
  - src/lib/ai/prompts/documentary.ts
  - src/lib/parsers/registry.ts
  - src/lib/parsers/txt-parser.ts
  - src/lib/types/__tests__/project-types.test.ts
  - src/lib/parsers/__tests__/txt-parser.test.ts
  - src/lib/ai/schemas/__tests__/documentary.test.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/app-sidebar.tsx
  - src/components/placeholder-page.tsx
  - src/app/page.tsx
  - src/components/project-type-tabs.tsx
autonomous: true
requirements: [CORE-01, PARSE-01, ANLYS-01]

must_haves:
  truths:
    - "Project type registry returns config for 'documentary' with correct accepted extensions, schema, and prompt"
    - "Plain text parser extracts text, word count, and line count from a .txt string"
    - "Documentary analysis Zod schema validates a well-formed analysis object and rejects malformed ones"
    - "App shell renders sidebar with navigation items and main content area"
  artifacts:
    - path: "src/lib/types/project-types.ts"
      provides: "Project type registry with documentary config"
      exports: ["PROJECT_TYPES", "ProjectTypeConfig"]
    - path: "src/lib/ai/schemas/documentary.ts"
      provides: "Zod schema for documentary analysis output"
      exports: ["documentaryAnalysisSchema", "DocumentaryAnalysis"]
    - path: "src/lib/ai/prompts/documentary.ts"
      provides: "System prompt for documentary interview mining"
      exports: ["documentarySystemPrompt"]
    - path: "src/lib/parsers/txt-parser.ts"
      provides: "Plain text parser with metadata extraction"
      exports: ["parseTxt"]
    - path: "src/lib/parsers/registry.ts"
      provides: "Parser registry mapping file extensions to parsers"
      exports: ["parseFile"]
    - path: "src/components/app-sidebar.tsx"
      provides: "Sidebar navigation component"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
  key_links: []
---

<objective>
Scaffold the Next.js project, install all dependencies, create the foundational type system (project type registry, documentary analysis schema, text parser), set up test infrastructure, and build the app shell layout with sidebar navigation.

Purpose: Establish the project skeleton and all shared contracts that Plans 02 and 03 build on. Output-first: define the documentary analysis schema and project type registry before building upload or analysis flows.
Output: Working Next.js app with test infra, type contracts, parser, and app shell layout.
</objective>

<execution_context>
@C:/Users/bplan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vertical-slice/01-RESEARCH.md
@.planning/phases/01-vertical-slice/01-UI-SPEC.md
@.planning/phases/01-vertical-slice/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install all dependencies</name>
  <files>package.json, tsconfig.json, vitest.config.ts, src/app/globals.css</files>
  <read_first>
    .planning/phases/01-vertical-slice/01-RESEARCH.md (Standard Stack section — exact package names and versions)
    .planning/phases/01-vertical-slice/01-UI-SPEC.md (Design System section — shadcn components list, Color section — CSS variables)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --yes --src-dir` in the project root (F:/AI/GSD/FilmIntern). This scaffolds Next.js 16 with TypeScript, Tailwind CSS 4, ESLint, App Router, Turbopack, and src/ directory.

    2. Initialize shadcn/ui: `npx shadcn@latest init` — accept defaults (New York style, neutral base color).

    3. Add shadcn components: `npx shadcn@latest add button card tabs badge accordion separator skeleton select tooltip`

    4. Install AI/LLM dependencies: `npm install ai @ai-sdk/anthropic @ai-sdk/react zod`

    5. Install file upload: `npm install react-dropzone`

    6. Install dev/test dependencies: `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`

    7. Create `vitest.config.ts` in project root:
    ```typescript
    import { defineConfig } from 'vitest/config';
    import path from 'path';

    export default defineConfig({
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: [],
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
    });
    ```

    8. Add test scripts to `package.json`:
    - `"test": "vitest run --reporter=verbose"`
    - `"test:watch": "vitest --reporter=verbose"`

    9. Update `src/app/globals.css` to include custom CSS variables for the design system. After the existing Tailwind imports, add a `:root` block with these custom properties:
    - `--km-emotional: #F43F5E;`
    - `--km-revelation: #F59E0B;`
    - `--km-contradiction: #F97316;`
    - `--km-humor: #10B981;`

    Keep all existing shadcn/Tailwind CSS that create-next-app and shadcn init generated. Only ADD the custom key moment variables.

    10. Verify the scaffold works: `npm run build` should succeed. `npm run test` should run (0 tests).
  </action>
  <verify>
    <automated>cd "F:/AI/GSD/FilmIntern" && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"ai"`, `"@ai-sdk/anthropic"`, `"@ai-sdk/react"`, `"zod"`, `"react-dropzone"` in dependencies
    - `package.json` contains `"vitest"`, `"@testing-library/react"` in devDependencies
    - `package.json` contains `"test": "vitest run --reporter=verbose"` in scripts
    - `vitest.config.ts` exists with `environment: 'jsdom'` and `'@': path.resolve(__dirname, './src')`
    - `src/app/globals.css` contains `--km-emotional`
    - `src/components/ui/button.tsx` exists (shadcn installed)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project scaffolded with all dependencies, shadcn/ui initialized with required components, vitest configured, custom CSS variables added, build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create project type registry, documentary schema, prompt, and text parser with tests</name>
  <files>
    src/lib/types/project-types.ts,
    src/lib/ai/schemas/documentary.ts,
    src/lib/ai/prompts/documentary.ts,
    src/lib/parsers/registry.ts,
    src/lib/parsers/txt-parser.ts,
    src/lib/types/__tests__/project-types.test.ts,
    src/lib/parsers/__tests__/txt-parser.test.ts,
    src/lib/ai/schemas/__tests__/documentary.test.ts
  </files>
  <read_first>
    .planning/phases/01-vertical-slice/01-RESEARCH.md (Pattern 1: Output-First Design — documentaryAnalysisSchema, Pattern 4: Project Type Registry, Documentary System Prompt code example)
    .planning/phases/01-vertical-slice/01-UI-SPEC.md (Sidebar Navigation Items — icon names for project types)
  </read_first>
  <behavior>
    project-types.test.ts:
    - PROJECT_TYPES['documentary'] returns object with id='documentary', label='Documentary', acceptedExtensions=['.txt']
    - PROJECT_TYPES['documentary'].icon equals 'Video'
    - Object.keys(PROJECT_TYPES) has length >= 1

    txt-parser.test.ts:
    - parseTxt("Hello world\nSecond line") returns { text: "Hello world\nSecond line", metadata: { wordCount: 4, lineCount: 2, format: 'txt' } }
    - parseTxt("") returns { text: "", metadata: { wordCount: 0, lineCount: 1, format: 'txt' } }
    - parseTxt with large text (1000+ words) returns correct wordCount

    documentary.test.ts:
    - documentaryAnalysisSchema.safeParse on a valid complete object returns success=true
    - documentaryAnalysisSchema.safeParse on object missing 'keyQuotes' returns success=false
    - documentaryAnalysisSchema.safeParse on object with invalid quote category returns success=false
  </behavior>
  <action>
    Write tests FIRST (RED), then implement (GREEN).

    **src/lib/types/project-types.ts:**
    Export `ProjectTypeConfig` interface with fields: id (string), label (string), description (string), icon (string — Lucide icon name), acceptedExtensions (string[]), acceptedMimeTypes (string[]), fileTypeLabel (string).
    NOTE: Do NOT include `analysisSchema` or `systemPrompt` in the interface — these create circular import issues with Zod. Keep the registry as pure config data. Analysis schema and prompt are resolved separately by project type ID.

    Export `PROJECT_TYPES` as `Record<string, ProjectTypeConfig>` with one entry:
    - key: 'documentary'
    - id: 'documentary', label: 'Documentary', description: 'Interview mining: extract quotes, themes, and key moments from transcripts', icon: 'Video', acceptedExtensions: ['.txt'], acceptedMimeTypes: ['text/plain'], fileTypeLabel: 'transcript'

    Also add placeholder entries (not selectable in Phase 1) for future project types:
    - 'corporate': label 'Corporate Interview', icon 'Briefcase'
    - 'narrative': label 'Narrative Film', icon 'Film'
    - 'tv-episodic': label 'TV / Episodic', icon 'Tv'
    - 'short-form': label 'Short-form / Branded', icon 'Clapperboard'
    Each placeholder has acceptedExtensions: [] and fileTypeLabel: 'script'.

    **src/lib/ai/schemas/documentary.ts:**
    Implement the `documentaryAnalysisSchema` Zod object EXACTLY as shown in the research doc Pattern 1 (the full schema with summary, keyQuotes, recurringThemes, keyMoments, editorialNotes). Export the schema and the inferred type `DocumentaryAnalysis`.

    **src/lib/ai/prompts/documentary.ts:**
    Export `documentarySystemPrompt` as a string constant. Use the EXACT prompt text from the research doc "Documentary System Prompt (Core IP)" code example. This is the professional documentary editing framework prompt.

    **src/lib/parsers/txt-parser.ts:**
    Export `parseTxt(content: string): ParseResult` where `ParseResult` is:
    ```typescript
    export interface ParseResult {
      text: string;
      metadata: {
        wordCount: number;
        lineCount: number;
        format: string;
        filename?: string;
        size?: number;
      };
    }
    ```
    Implementation: return the raw text, count words by splitting on /\s+/ and filtering empty strings, count lines by splitting on '\n'.

    **src/lib/parsers/registry.ts:**
    Export `parseFile(content: string, filename: string): ParseResult`. Determine parser by file extension. For '.txt', delegate to `parseTxt`. For unknown extensions, throw an error `Unsupported file format: {ext}`. Include the filename in the returned metadata.
  </action>
  <verify>
    <automated>cd "F:/AI/GSD/FilmIntern" && npx vitest run --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types/project-types.ts` contains `export const PROJECT_TYPES` and `export interface ProjectTypeConfig`
    - `src/lib/types/project-types.ts` contains `documentary` key with `acceptedExtensions: ['.txt']`
    - `src/lib/ai/schemas/documentary.ts` contains `export const documentaryAnalysisSchema` and `export type DocumentaryAnalysis`
    - `src/lib/ai/schemas/documentary.ts` contains `keyQuotes`, `recurringThemes`, `keyMoments`, `editorialNotes` fields
    - `src/lib/ai/prompts/documentary.ts` contains `export const documentarySystemPrompt`
    - `src/lib/ai/prompts/documentary.ts` contains `NEVER invent or paraphrase quotes`
    - `src/lib/parsers/txt-parser.ts` contains `export function parseTxt` and `export interface ParseResult`
    - `src/lib/parsers/registry.ts` contains `export function parseFile`
    - All 3 test files exist and pass: `npx vitest run` shows 0 failures
  </acceptance_criteria>
  <done>Project type registry with documentary config, documentary analysis Zod schema, system prompt, text parser, and parser registry all implemented with passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Build app shell layout with sidebar navigation and project type tabs</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/components/app-sidebar.tsx,
    src/components/project-type-tabs.tsx,
    src/components/placeholder-page.tsx
  </files>
  <read_first>
    src/app/layout.tsx (current scaffolded layout — preserve next/font setup, modify structure)
    src/app/globals.css (current CSS — know what theme tokens exist)
    src/lib/types/project-types.ts (project type registry — use for tab generation)
    .planning/phases/01-vertical-slice/01-UI-SPEC.md (Layout Contract — sidebar 240px, breadcrumb bar, tabs; Sidebar Navigation Items — icons and states; Color section — all color values; Copywriting Contract — exact copy strings)
  </read_first>
  <action>
    **src/app/layout.tsx:**
    Update the root layout to use Inter font via `next/font/google` with weights [400, 600]. Set the layout structure:
    - Flex row container, full viewport height
    - `<AppSidebar />` on the left (240px fixed width)
    - Main area on the right (flex-1) containing a breadcrumb bar and scrollable content area
    - Breadcrumb bar: height 48px, border-bottom, flex row with left text "Projects" and right placeholder avatar circle (32px, bg-muted, rounded-full)
    - Main content: max-width 1200px, mx-auto, px-8 (32px), py-6 (24px)
    - Import and wrap children with project type tabs

    **src/components/app-sidebar.tsx:**
    'use client' component. 240px fixed width, dark background (bg-stone-900 or similar matching --sidebar #1C1917).

    Content top-to-bottom:
    1. Brand area: "Nano Banana" text in Display size (28px/600), text-stone-50, padding 24px, border-b border-stone-800
    2. Nav items list (ul), padding 8px. Each item is a button/link with:
       - Height 40px, flex row, gap 12px, px 12px, rounded-md
       - Lucide icon (20px) + label text (13px/400)
       - Text color: text-stone-400 default, text-stone-50 for active
       - Active item ("Projects"): left 3px border accent (#D97706), bg-white/5
       - Hover: bg-white/10
       - Disabled items (Dashboard, Shot Lists, Image Prompts, Exports): opacity-50, cursor-default, no hover effect
    3. Settings pinned to bottom (mt-auto), same style as nav items, disabled

    Nav items in order (from UI-SPEC):
    - Dashboard (LayoutDashboard) — disabled
    - Projects (FolderOpen) — active
    - Shot Lists (ListVideo) — disabled
    - Image Prompts (ImagePlus) — disabled
    - Exports (Download) — disabled
    - Settings (Settings) — disabled, bottom-pinned

    **src/components/project-type-tabs.tsx:**
    'use client' component. Horizontal tab bar using shadcn Tabs component.
    - Tab values from PROJECT_TYPES registry keys: documentary, corporate, narrative, tv-episodic, short-form
    - Default value: 'documentary'
    - Active tab gets accent underline style
    - Non-documentary tabs: when selected, show `<PlaceholderPage />` with heading "Coming Soon" and body "This project type is not yet available. Documentary analysis is ready to use."
    - Documentary tab: renders children (the actual upload/analysis content)
    - Tab labels from PROJECT_TYPES[key].label

    **src/components/placeholder-page.tsx:**
    Simple centered empty state card with heading and body text. Props: `heading: string`, `body: string`. Uses shadcn Card, centered in the content area with muted foreground text.

    **src/app/page.tsx:**
    Replace the default Next.js page. Import ProjectTypeTabs. Inside the documentary tab content area, render a placeholder for now: "Select Documentary to begin. Upload your transcript to get started." This will be replaced by Plan 02 (upload flow).

    Responsive behavior (from UI-SPEC):
    - >= 1280px: full 240px sidebar
    - 1024-1279px: sidebar collapses to 64px icon-only (hide labels, show only icons)
    - < 1024px: sidebar hidden (hamburger menu NOT implemented in Phase 1 — just hide sidebar)
    Use Tailwind responsive classes: sidebar div gets `hidden lg:flex lg:w-16 xl:w-60` and label spans get `hidden xl:inline`.
  </action>
  <verify>
    <automated>cd "F:/AI/GSD/FilmIntern" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/app-sidebar.tsx` contains `Nano Banana` and `FolderOpen` and `LayoutDashboard`
    - `src/components/app-sidebar.tsx` contains `bg-white/5` (active state) and `opacity-50` (disabled state)
    - `src/components/project-type-tabs.tsx` contains `PROJECT_TYPES` import and `documentary` tab value
    - `src/components/placeholder-page.tsx` contains props `heading` and `body`
    - `src/app/layout.tsx` contains `AppSidebar` import and Inter font with weights `['400', '600']`
    - `src/app/page.tsx` contains `ProjectTypeTabs` import
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>App shell with dark sidebar (brand, nav items with correct icons/states), project type tabs (documentary active, others showing placeholder), responsive collapse, and breadcrumb bar — all rendering and building without errors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npx vitest run --reporter=verbose` — all tests pass (project type registry, txt parser, documentary schema)
- Dev server (`npm run dev`) shows app shell with sidebar, tabs, and placeholder content
</verification>

<success_criteria>
- Next.js 16 project fully scaffolded with all dependencies
- Project type registry exports documentary config with correct fields
- Documentary analysis Zod schema validates structured output shape
- Documentary system prompt contains professional editing framework
- Plain text parser extracts text and metadata (word count, line count)
- App shell renders sidebar with brand, nav items, and project type tabs
- All unit tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-vertical-slice/01-01-SUMMARY.md`
</output>
