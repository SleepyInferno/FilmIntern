---
phase: 04-export-and-document-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/api/documents/generate/route.ts
  - src/app/api/documents/generate/__tests__/route.test.ts
  - src/lib/documents/generators/index.ts
  - src/lib/documents/generators/prompts.ts
  - src/components/document-workspace.tsx
  - src/components/__tests__/document-workspace.test.tsx
  - src/components/analysis-report.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - OUTP-03
must_haves:
  truths:
    - "After analysis, the user can generate an allowed derivative document for the selected project type."
    - "Generated documents open as editable tabs beside the report instead of a separate workspace."
    - "Narrative and TV outlines expose both beat and scene-by-scene generation options."
  artifacts:
    - path: "src/app/api/documents/generate/route.ts"
      provides: "Server route for outline, treatment, and proposal generation"
    - path: "src/components/document-workspace.tsx"
      provides: "Tabbed editor surface for report and generated documents"
    - path: "src/app/page.tsx"
      provides: "Client state wiring for generation buttons, tabs, and edited document state"
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/documents/generate"
      via: "fetch POST when user clicks Generate buttons"
      pattern: "/api/documents/generate"
    - from: "src/components/document-workspace.tsx"
      to: "src/lib/documents/types.ts"
      via: "GeneratedDocument props and edit callbacks"
      pattern: "GeneratedDocument"
    - from: "src/app/api/documents/generate/route.ts"
      to: "src/lib/documents/availability.ts"
      via: "document kind validation"
      pattern: "supportsDocumentKind"
---

<objective>
Build the derivative document generation flow and in-app editing workspace so users can create, review, and revise outlines, treatments, and proposal-style outputs.

Purpose: Satisfy OUTP-03 with a concrete post-analysis workflow that matches the locked product decisions about tabs, editability, and project-type-specific document choices.
Output: Generation API route, prompt registry, editable document workspace, and page wiring that adds post-analysis document tabs and action buttons.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-export-and-document-generation/04-CONTEXT.md
@.planning/phases/04-export-and-document-generation/04-RESEARCH.md
@.planning/phases/04-export-and-document-generation/04-VALIDATION.md
@src/app/page.tsx
@src/components/analysis-report.tsx
@src/app/api/analyze/route.ts
@src/lib/types/project-types.ts
@src/lib/documents/types.ts
@src/lib/documents/availability.ts
@src/lib/documents/report-document.ts

<interfaces>
From src/lib/documents/types.ts:
```typescript
type DocumentKind = 'report' | 'outline' | 'treatment' | 'proposal';
type OutlineMode = 'beats' | 'scene-by-scene';
type ExportFormat = 'pdf' | 'docx';

interface GeneratedDocument {
  id: string;
  kind: DocumentKind;
  projectType: string;
  title: string;
  writtenBy: string;
  createdAt: string;
  updatedAt: string;
  outlineMode?: OutlineMode;
  cover: DocumentCover;
  content: Record<string, unknown>;
  quoteRefs: DocumentQuoteRef[];
  sourceText: string;
  analysisSnapshot?: Record<string, unknown>;
}
```

From src/app/api/analyze/route.ts:
```typescript
export async function POST(req: Request)
```

From src/app/page.tsx:
```typescript
const [uploadData, setUploadData] = useState<UploadData | null>(null);
const [analysisData, setAnalysisData] = useState<Partial<DocumentaryAnalysis> | null>(null);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the document generation and workspace behavior with tests</name>
  <read_first>
    - src/app/page.tsx
    - src/components/analysis-report.tsx
    - src/app/api/analyze/__tests__/route.test.ts
    - src/components/__tests__/analysis-report.test.ts
    - src/lib/documents/types.ts
    - src/lib/documents/availability.ts
    - .planning/phases/04-export-and-document-generation/04-VALIDATION.md
  </read_first>
  <files>src/app/api/documents/generate/__tests__/route.test.ts, src/components/__tests__/document-workspace.test.tsx</files>
  <behavior>
    - Test 1: POST `/api/documents/generate` rejects unknown project types and disallowed document kinds with status `400`.
    - Test 2: POST `/api/documents/generate` accepts `{ projectType: 'documentary', documentKind: 'proposal' }` and returns a `GeneratedDocument`.
    - Test 3: POST `/api/documents/generate` accepts `{ projectType: 'narrative', documentKind: 'outline', outlineMode: 'scene-by-scene' }` and returns `outlineMode: 'scene-by-scene'`.
    - Test 4: `DocumentWorkspace` renders `Report`, generated document tabs, a rich-text editor shell, and top-right export dropdown trigger text.
    - Test 5: clicking a quote reference invokes the provided jump callback with the quote ID.
  </behavior>
  <action>Create `src/app/api/documents/generate/__tests__/route.test.ts` and `src/components/__tests__/document-workspace.test.tsx` before implementation. The route test must build `Request` bodies with the exact JSON shape `{ projectType, documentKind, sourceText, title, writtenBy, analysis, outlineMode? }`, assert status `400` plus JSON error messages `Unsupported project type` and `Document kind not available for project type`, and assert successful responses include keys `id`, `kind`, `projectType`, `content`, and `quoteRefs`. The component test must assert tabs labeled `Report` and a generated document title, buttons labeled `Generate Outline`, `Generate Treatment`, or `Generate Proposal` based on props, an editable surface with `data-testid="document-editor"`, and an export trigger labeled `Export` in the top-right action row.</action>
  <acceptance_criteria>
    - `rg "Document kind not available for project type" src/app/api/documents/generate/__tests__/route.test.ts` returns one match.
    - `rg "scene-by-scene" src/app/api/documents/generate/__tests__/route.test.ts` returns at least one match.
    - `rg "data-testid=\\\"document-editor\\\"" src/components/__tests__/document-workspace.test.tsx` returns one match.
    - `rg "Generate Proposal" src/components/__tests__/document-workspace.test.tsx` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/documents/generate/__tests__/route.test.ts src/components/__tests__/document-workspace.test.tsx</automated>
  </verify>
  <done>Phase 4 generation and editing behavior is pinned down with executable tests before the route and workspace are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the project-type-aware generation route and prompt registry</name>
  <read_first>
    - src/app/api/analyze/route.ts
    - src/lib/documents/types.ts
    - src/lib/documents/availability.ts
    - src/lib/documents/report-document.ts
    - .planning/phases/04-export-and-document-generation/04-RESEARCH.md
  </read_first>
  <files>src/app/api/documents/generate/route.ts, src/lib/documents/generators/index.ts, src/lib/documents/generators/prompts.ts</files>
  <behavior>
    - Test 1: documentary and corporate requests can generate `proposal`.
    - Test 2: narrative and tv-episodic requests can generate `treatment`.
    - Test 3: outline generation preserves `outlineMode` values `beats` and `scene-by-scene`.
  </behavior>
  <action>Create `src/app/api/documents/generate/route.ts` with a `POST` handler that validates JSON using `zod` and accepts the exact payload `{ projectType: string; documentKind: 'outline' | 'treatment' | 'proposal'; sourceText: string; title: string; writtenBy: string; analysis: Record<string, unknown>; outlineMode?: 'beats' | 'scene-by-scene'; }`. The route must return `400` with `Unsupported project type` for unknown project types, `400` with `Document kind not available for project type` when `supportsDocumentKind()` fails, and `400` with `Outline mode not allowed for project type` when `outlineMode === 'scene-by-scene'` is used outside `narrative` or `tv-episodic`. Create `src/lib/documents/generators/prompts.ts` exporting exact prompt builders for `outline`, `treatment`, and `proposal` that embed both `sourceText` and serialized `analysis`. Create `src/lib/documents/generators/index.ts` exporting `generateDocument(input)` which calls Anthropic through `generateText` or `streamText`, builds a `GeneratedDocument`, sets `cover.typeLabel` to `Outline`, `Treatment`, or `Strategic Proposal`, keeps `sourceText`, stores `analysisSnapshot`, and returns Tiptap JSON content. Documentary and corporate proposals must use a polished strategic-summary/proposal framing, while narrative and tv-episodic treatments must stay screenplay-oriented. All document IDs must use `crypto.randomUUID()`.</action>
  <acceptance_criteria>
    - `rg "Document kind not available for project type" src/app/api/documents/generate/route.ts` returns one match.
    - `rg "Outline mode not allowed for project type" src/app/api/documents/generate/route.ts` returns one match.
    - `rg "Strategic Proposal" src/lib/documents/generators/index.ts` returns one match.
    - `rg "crypto.randomUUID" src/lib/documents/generators/index.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/documents/generate/__tests__/route.test.ts</automated>
  </verify>
  <done>The server can generate valid outline, treatment, and proposal documents with project-type-specific availability rules and consistent GeneratedDocument output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the tabbed document workspace and editing flow into the main page</name>
  <read_first>
    - src/app/page.tsx
    - src/components/analysis-report.tsx
    - src/components/project-type-tabs.tsx
    - src/components/ui/tabs.tsx
    - src/lib/documents/types.ts
    - src/lib/documents/availability.ts
  </read_first>
  <files>src/components/document-workspace.tsx, src/components/analysis-report.tsx, src/app/page.tsx</files>
  <behavior>
    - Test 1: the report appears as the first tab and generated documents render as additional tabs.
    - Test 2: Generate buttons appear only for allowed document kinds.
    - Test 3: editor changes update the in-memory `GeneratedDocument` state that later export requests will use.
  </behavior>
  <action>Create `src/components/document-workspace.tsx` as a client component that receives `{ projectType, reportDocument, generatedDocuments, activeDocumentId, onActiveDocumentChange, onGenerateDocument, onUpdateDocument, onQuoteJump, onExport }`. Use the existing `Tabs`, `TabsList`, `TabsTrigger`, and `TabsContent` primitives to render `Report` plus one tab per generated document. Add a top-right action row with one primary `Export` button opening a format dropdown containing `PDF (.pdf)` and `Word (.docx)` entries, and secondary generation buttons labeled exactly `Generate Outline`, `Generate Treatment`, and `Generate Proposal` according to `getAvailableDocumentKinds(projectType)`. Mount a simple Tiptap editor for generated documents with `data-testid="document-editor"`, update the matching `GeneratedDocument.content` on every change, and render report content read-only when the `Report` tab is active. Update `src/components/analysis-report.tsx` so its top area accepts an optional action slot for the new export control placement. Update `src/app/page.tsx` to replace the single report render with state for `reportDocument`, `generatedDocuments`, `activeDocumentId`, `writtenBy`, and `title`, build the report document after analysis completes via `buildReportDocument`, call `/api/documents/generate` when the user clicks a generation button, and keep everything inside the current documentary workflow instead of creating a separate page.</action>
  <acceptance_criteria>
    - `rg "Generate Outline" src/components/document-workspace.tsx` returns one match.
    - `rg "PDF \\(\\.pdf\\)" src/components/document-workspace.tsx` returns one match.
    - `rg "data-testid=\\\"document-editor\\\"" src/components/document-workspace.tsx` returns one match.
    - `rg "/api/documents/generate" src/app/page.tsx` returns one match.
    - `rg "action" src/components/analysis-report.tsx` returns one match for the new action prop or slot implementation.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/document-workspace.test.tsx</automated>
  </verify>
  <done>The analysis page now presents the report and generated documents in one tabbed workspace, lets the user edit generated content in-app, and surfaces only the allowed document-generation actions for the current project type.</done>
</task>

</tasks>

<verification>
Run the route and component test files, then `npm test`, and manually inspect the documentary flow in the browser to confirm tab switching and edit persistence work end-to-end.
</verification>

<success_criteria>
Users can generate derivative documents from the analysis workflow, edit them in-app, and keep report plus generated outputs in one coherent tabbed workspace with project-type-aware actions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-document-generation/04-02-SUMMARY.md`
</output>
