---
phase: 04-export-and-document-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/lib/documents/export-request.ts
  - src/lib/documents/export-layout.ts
  - src/lib/documents/render-document-html.ts
  - src/lib/documents/export-pdf.ts
  - src/lib/documents/export-docx.ts
  - src/lib/documents/__tests__/pdf-template.test.ts
  - src/lib/documents/__tests__/docx-export.test.ts
autonomous: true
requirements:
  - OUTP-02
must_haves:
  truths:
    - "The export pipeline can turn canonical document state into PDF and DOCX bytes without depending on UI markup."
    - "Exported files include a cover page with Title, Type, Date, and Written by."
    - "Narrative and TV exports use screenplay-oriented layout rules while documentary and corporate exports use professional document formatting."
  artifacts:
    - path: "src/lib/documents/export-request.ts"
      provides: "Shared GeneratedDocument request validation and filename sanitization for export consumers"
    - path: "src/lib/documents/export-layout.ts"
      provides: "Shared layout-profile selection for PDF and DOCX"
    - path: "src/lib/documents/render-document-html.ts"
      provides: "HTML rendering from GeneratedDocument content for PDF printing"
    - path: "src/lib/documents/export-pdf.ts"
      provides: "PDF byte generation from rendered export HTML"
    - path: "src/lib/documents/export-docx.ts"
      provides: "DOCX byte generation from GeneratedDocument content"
  key_links:
    - from: "src/lib/documents/render-document-html.ts"
      to: "src/lib/documents/export-layout.ts"
      via: "HTML renderer applies the shared layout profile to canonical document content"
      pattern: "getLayoutProfile"
    - from: "src/lib/documents/export-pdf.ts"
      to: "src/lib/documents/render-document-html.ts"
      via: "PDF exporter prints the generated HTML"
      pattern: "renderDocumentHtml"
    - from: "src/lib/documents/export-pdf.ts"
      to: "playwright"
      via: "exporter calls Chromium page.pdf on generated HTML"
      pattern: "page\\.pdf"
    - from: "src/lib/documents/export-docx.ts"
      to: "docx"
      via: "exporter builds Word output from the canonical document model"
      pattern: "Packer\\.toBuffer"
    - from: "src/lib/documents/export-request.ts"
      to: "src/lib/documents/types.ts"
      via: "request validation and filename generation operate on GeneratedDocument"
      pattern: "GeneratedDocument|generatedDocumentSchema"
---

<objective>
Implement the shared export pipeline internals that turn the edited canonical document state into polished PDF and DOCX deliverables.

Purpose: Satisfy the core formatting half of OUTP-02 without exporting the UI DOM, and keep PDF/DOCX formatting aligned through shared layout profiles and cover-page metadata before route wiring begins.
Output: Export request contract, HTML renderer, PDF/DOCX exporter libraries, and automated tests covering cover pages, quote labels, and layout profile selection.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-export-and-document-generation/04-CONTEXT.md
@.planning/phases/04-export-and-document-generation/04-RESEARCH.md
@.planning/phases/04-export-and-document-generation/04-VALIDATION.md
@src/lib/documents/types.ts
@src/lib/documents/report-document.ts

<interfaces>
From src/lib/documents/types.ts:
```typescript
type ExportFormat = 'pdf' | 'docx';

interface GeneratedDocument {
  id: string;
  kind: DocumentKind;
  projectType: string;
  title: string;
  writtenBy: string;
  createdAt: string;
  updatedAt: string;
  outlineMode?: OutlineMode;
  cover: DocumentCover;
  content: Record<string, unknown>;
  quoteRefs: DocumentQuoteRef[];
  sourceText: string;
  analysisSnapshot?: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exporter tests for PDF, DOCX, and layout profile selection</name>
  <read_first>
    - src/lib/documents/types.ts
    - .planning/phases/04-export-and-document-generation/04-RESEARCH.md
    - .planning/phases/04-export-and-document-generation/04-VALIDATION.md
    - vitest.config.ts
  </read_first>
  <files>src/lib/documents/__tests__/pdf-template.test.ts, src/lib/documents/__tests__/docx-export.test.ts</files>
  <behavior>
    - Test 1: layout profile selection returns `screenplay-document` for narrative/tv treatments and outlines, `coverage-report` for narrative/tv reports, and `professional-document` for documentary/corporate outputs.
    - Test 2: HTML rendering prepends cover labels `Title`, `Type`, `Date`, and `Written by` and includes quote labels like `Q1`.
    - Test 3: PDF export delegates to Playwright with `printBackground: true`.
    - Test 4: DOCX export preserves cover labels `Title`, `Type`, `Date`, and `Written by`, plus quote labels like `Q1`.
  </behavior>
  <action>Create the exporter-focused test files before implementation. `src/lib/documents/__tests__/pdf-template.test.ts` must assert the string layout profile outputs `coverage-report`, `screenplay-document`, and `professional-document`, must assert the rendered export HTML contains the literal cover labels `Title`, `Type`, `Date`, and `Written by`, and must verify the renderer includes at least one quote label such as `Q1`. `src/lib/documents/__tests__/docx-export.test.ts` must assert the DOCX exporter writes `Title`, `Type`, `Date`, `Written by`, and at least one `Q1` quote label into the generated document structure or mocked paragraph data, and that the PDF exporter path passes `printBackground: true` into Playwright `page.pdf()`.</action>
  <acceptance_criteria>
    - `rg "screenplay-document" src/lib/documents/__tests__/pdf-template.test.ts` returns one match.
    - `rg "Written by" src/lib/documents/__tests__/pdf-template.test.ts` returns one match.
    - `rg "Written by" src/lib/documents/__tests__/docx-export.test.ts` returns one match.
    - `rg "printBackground: true" src/lib/documents/__tests__/docx-export.test.ts src/lib/documents/__tests__/pdf-template.test.ts` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/documents/__tests__/pdf-template.test.ts src/lib/documents/__tests__/docx-export.test.ts</automated>
  </verify>
  <done>The shared export internals have executable coverage for layout decisions, cover-page metadata, quote-label preservation, and Playwright/DOCX integration points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared export contracts and PDF/DOCX exporters from canonical document state</name>
  <read_first>
    - src/lib/documents/types.ts
    - .planning/phases/04-export-and-document-generation/04-RESEARCH.md
    - .planning/phases/04-export-and-document-generation/04-CONTEXT.md
  </read_first>
  <files>src/lib/documents/export-request.ts, src/lib/documents/export-layout.ts, src/lib/documents/render-document-html.ts, src/lib/documents/export-pdf.ts, src/lib/documents/export-docx.ts</files>
  <behavior>
    - Test 1: HTML export rendering uses the edited `GeneratedDocument.content`, not UI markup.
    - Test 2: PDF export uses Playwright Chromium `page.pdf()` with `printBackground: true`.
    - Test 3: DOCX export uses `docx` `Document` and `Packer.toBuffer()` to build the file.
  </behavior>
  <action>Create `src/lib/documents/export-request.ts` to separate exporter internals from route exposure. It must export a `generatedDocumentSchema`/validator for the serialized `GeneratedDocument` payload plus a shared filename helper that converts `document.title` into a lowercase hyphenated stem. Create `src/lib/documents/export-layout.ts` exporting `getLayoutProfile(document)` that returns `coverage-report` for `document.projectType` values `narrative` and `tv-episodic` when `document.kind === 'report'`, returns `screenplay-document` for `narrative` and `tv-episodic` when `document.kind` is `outline` or `treatment`, and returns `professional-document` otherwise. Create `src/lib/documents/render-document-html.ts` that uses `@tiptap/static-renderer` to convert `document.content` into HTML, prepends a cover page with literal labels `Title`, `Type`, `Date`, and `Written by`, and appends a quote reference section listing each `label`, `speaker`, and quote text. Create `src/lib/documents/export-pdf.ts` exporting `exportPdf(document)` that launches Playwright Chromium, sets the generated HTML content, and returns PDF bytes from `page.pdf({ format: 'Letter', printBackground: true, margin: { top: '0.75in', right: '0.75in', bottom: '0.75in', left: '0.75in' } })`. Create `src/lib/documents/export-docx.ts` exporting `exportDocx(document)` that uses `docx` `Document`, `Paragraph`, `TextRun`, and `Packer.toBuffer()` to build a cover page, body sections, and a quote reference appendix that preserves labels like `Q1`.</action>
  <acceptance_criteria>
    - `rg "generatedDocumentSchema|GeneratedDocument" src/lib/documents/export-request.ts` returns at least one match.
    - `rg "coverage-report" src/lib/documents/export-layout.ts` returns one match.
    - `rg "screenplay-document" src/lib/documents/export-layout.ts` returns one match.
    - `rg "Written by" src/lib/documents/render-document-html.ts` returns one match.
    - `rg "printBackground: true" src/lib/documents/export-pdf.ts` returns one match.
    - `rg "Packer.toBuffer" src/lib/documents/export-docx.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/documents/__tests__/pdf-template.test.ts src/lib/documents/__tests__/docx-export.test.ts</automated>
  </verify>
  <done>Both export formats derive from the same GeneratedDocument state, share layout rules, and include the required cover metadata and quote labels.</done>
</task>

</tasks>

<verification>
Run the exporter test files, then `npm test`, so the shared export internals are stable before route exposure and browser download wiring begin.
</verification>

<success_criteria>
Phase 4 has a reusable export core that can render any canonical document into PDF or DOCX bytes with consistent cover-page metadata and project-type-aware layout rules, ready for dedicated route and client wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-document-generation/04-03-SUMMARY.md`
</output>
