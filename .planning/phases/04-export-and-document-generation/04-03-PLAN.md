---
phase: 04-export-and-document-generation
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/api/export/pdf/route.ts
  - src/app/api/export/docx/route.ts
  - src/app/api/export/__tests__/pdf.route.test.ts
  - src/app/api/export/__tests__/docx.route.test.ts
  - src/lib/documents/export-layout.ts
  - src/lib/documents/render-document-html.ts
  - src/lib/documents/export-pdf.ts
  - src/lib/documents/export-docx.ts
  - src/lib/documents/__tests__/pdf-template.test.ts
  - src/lib/documents/__tests__/docx-export.test.ts
autonomous: true
requirements:
  - OUTP-02
must_haves:
  truths:
    - "The user can export the current edited document as PDF or DOCX from the shared document state."
    - "Exported files include a cover page with Title, Type, Date, and Written by."
    - "Narrative and TV exports use screenplay-oriented layout rules while documentary and corporate exports use professional document formatting."
  artifacts:
    - path: "src/app/api/export/pdf/route.ts"
      provides: "PDF byte response route"
    - path: "src/app/api/export/docx/route.ts"
      provides: "DOCX byte response route"
    - path: "src/lib/documents/export-layout.ts"
      provides: "Shared layout-profile selection for PDF and DOCX"
    - path: "src/lib/documents/render-document-html.ts"
      provides: "HTML rendering from GeneratedDocument content for PDF printing"
  key_links:
    - from: "src/app/api/export/pdf/route.ts"
      to: "src/lib/documents/export-pdf.ts"
      via: "route handler calls PDF exporter with request payload"
      pattern: "exportPdf"
    - from: "src/app/api/export/docx/route.ts"
      to: "src/lib/documents/export-docx.ts"
      via: "route handler calls DOCX exporter with request payload"
      pattern: "exportDocx"
    - from: "src/lib/documents/export-layout.ts"
      to: "src/lib/documents/types.ts"
      via: "layout chosen from projectType and document kind"
      pattern: "GeneratedDocument"
---

<objective>
Implement the export pipeline that turns the edited canonical document state into polished PDF and DOCX deliverables.

Purpose: Satisfy OUTP-02 without exporting the UI DOM, and keep PDF/DOCX formatting aligned through shared layout profiles and cover-page metadata.
Output: PDF and DOCX export libraries, export API routes, and automated tests covering headers, cover pages, and layout profile selection.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-export-and-document-generation/04-CONTEXT.md
@.planning/phases/04-export-and-document-generation/04-RESEARCH.md
@.planning/phases/04-export-and-document-generation/04-VALIDATION.md
@src/lib/documents/types.ts
@src/lib/documents/report-document.ts
@src/components/analysis-report.tsx

<interfaces>
From src/lib/documents/types.ts:
```typescript
type ExportFormat = 'pdf' | 'docx';

interface GeneratedDocument {
  id: string;
  kind: DocumentKind;
  projectType: string;
  title: string;
  writtenBy: string;
  createdAt: string;
  updatedAt: string;
  outlineMode?: OutlineMode;
  cover: DocumentCover;
  content: Record<string, unknown>;
  quoteRefs: DocumentQuoteRef[];
  sourceText: string;
  analysisSnapshot?: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export tests for PDF, DOCX, and layout profile selection</name>
  <read_first>
    - src/lib/documents/types.ts
    - .planning/phases/04-export-and-document-generation/04-RESEARCH.md
    - .planning/phases/04-export-and-document-generation/04-VALIDATION.md
    - vitest.config.ts
  </read_first>
  <files>src/app/api/export/__tests__/pdf.route.test.ts, src/app/api/export/__tests__/docx.route.test.ts, src/lib/documents/__tests__/pdf-template.test.ts, src/lib/documents/__tests__/docx-export.test.ts</files>
  <behavior>
    - Test 1: POST `/api/export/pdf` returns status `200`, `Content-Type: application/pdf`, and `Content-Disposition` ending in `.pdf`.
    - Test 2: POST `/api/export/docx` returns status `200`, `Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document`, and `Content-Disposition` ending in `.docx`.
    - Test 3: layout profile selection returns `screenplay-document` for narrative/tv treatments and outlines, `coverage-report` for narrative/tv reports, and `professional-document` for documentary/corporate outputs.
    - Test 4: DOCX export preserves cover labels `Title`, `Type`, `Date`, and `Written by`, plus quote labels like `Q1`.
  </behavior>
  <action>Create the four test files before implementation. The route tests must send the exact body `{ document: GeneratedDocument }`, mock the exporter functions, and assert the route headers exactly `application/pdf` and `application/vnd.openxmlformats-officedocument.wordprocessingml.document`. The template tests must assert the string layout profile outputs `coverage-report`, `screenplay-document`, and `professional-document`, and the DOCX exporter test must assert the implementation writes `Title`, `Type`, `Date`, `Written by`, and at least one `Q1` quote label into the generated document structure or mocked paragraph data.</action>
  <acceptance_criteria>
    - `rg "application/pdf" src/app/api/export/__tests__/pdf.route.test.ts` returns one match.
    - `rg "application/vnd.openxmlformats-officedocument.wordprocessingml.document" src/app/api/export/__tests__/docx.route.test.ts` returns one match.
    - `rg "screenplay-document" src/lib/documents/__tests__/pdf-template.test.ts` returns one match.
    - `rg "Written by" src/lib/documents/__tests__/docx-export.test.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/export/__tests__/pdf.route.test.ts src/app/api/export/__tests__/docx.route.test.ts src/lib/documents/__tests__/pdf-template.test.ts src/lib/documents/__tests__/docx-export.test.ts</automated>
  </verify>
  <done>The export surface has executable coverage for HTTP responses, shared layout decisions, cover-page metadata, and quote-label preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared PDF and DOCX exporters from canonical document state</name>
  <read_first>
    - src/lib/documents/types.ts
    - .planning/phases/04-export-and-document-generation/04-RESEARCH.md
    - .planning/phases/04-export-and-document-generation/04-CONTEXT.md
  </read_first>
  <files>src/lib/documents/export-layout.ts, src/lib/documents/render-document-html.ts, src/lib/documents/export-pdf.ts, src/lib/documents/export-docx.ts</files>
  <behavior>
    - Test 1: HTML export rendering uses the edited `GeneratedDocument.content`, not UI markup.
    - Test 2: PDF export uses Playwright Chromium `page.pdf()` with `printBackground: true`.
    - Test 3: DOCX export uses `docx` `Document` and `Packer.toBuffer()` to build the file.
  </behavior>
  <action>Create `src/lib/documents/export-layout.ts` exporting `getLayoutProfile(document)` that returns `coverage-report` for `document.projectType` values `narrative` and `tv-episodic` when `document.kind === 'report'`, returns `screenplay-document` for `narrative` and `tv-episodic` when `document.kind` is `outline` or `treatment`, and returns `professional-document` otherwise. Create `src/lib/documents/render-document-html.ts` that uses `@tiptap/static-renderer` to convert `document.content` into HTML, prepends a cover page with literal labels `Title`, `Type`, `Date`, and `Written by`, and appends a quote reference section listing each `label`, `speaker`, and quote text. Create `src/lib/documents/export-pdf.ts` exporting `exportPdf(document)` that launches Playwright Chromium, sets the generated HTML content, and returns PDF bytes from `page.pdf({ format: 'Letter', printBackground: true, margin: { top: '0.75in', right: '0.75in', bottom: '0.75in', left: '0.75in' } })`. Create `src/lib/documents/export-docx.ts` exporting `exportDocx(document)` that uses `docx` `Document`, `Paragraph`, `TextRun`, and `Packer.toBuffer()` to build a cover page, body sections, and a quote reference appendix that preserves labels like `Q1`.</action>
  <acceptance_criteria>
    - `rg "coverage-report" src/lib/documents/export-layout.ts` returns one match.
    - `rg "screenplay-document" src/lib/documents/export-layout.ts` returns one match.
    - `rg "Written by" src/lib/documents/render-document-html.ts` returns one match.
    - `rg "printBackground: true" src/lib/documents/export-pdf.ts` returns one match.
    - `rg "Packer.toBuffer" src/lib/documents/export-docx.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/documents/__tests__/pdf-template.test.ts src/lib/documents/__tests__/docx-export.test.ts</automated>
  </verify>
  <done>Both export formats derive from the same GeneratedDocument state, share layout rules, and include the required cover metadata and quote labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose PDF and DOCX export routes for the edited document payload</name>
  <read_first>
    - src/lib/documents/export-pdf.ts
    - src/lib/documents/export-docx.ts
    - src/lib/documents/types.ts
    - src/app/api/analyze/route.ts
  </read_first>
  <files>src/app/api/export/pdf/route.ts, src/app/api/export/docx/route.ts</files>
  <behavior>
    - Test 1: the PDF route accepts a serialized `GeneratedDocument` and returns PDF bytes with a `.pdf` filename.
    - Test 2: the DOCX route accepts a serialized `GeneratedDocument` and returns DOCX bytes with a `.docx` filename.
  </behavior>
  <action>Create `src/app/api/export/pdf/route.ts` and `src/app/api/export/docx/route.ts` with `POST` handlers that parse `{ document }` JSON, validate the payload against the GeneratedDocument shape, call `exportPdf(document)` or `exportDocx(document)`, and return `new Response(buffer, { status: 200, headers: { 'Content-Type': ..., 'Content-Disposition': 'attachment; filename=\"' + sanitizedTitle + '.pdf\"' } })`. The PDF route must use `application/pdf`; the DOCX route must use `application/vnd.openxmlformats-officedocument.wordprocessingml.document`. Use `document.title` lowercased and hyphenated for the filename stem. Do not read from the DOM or regenerate the document from analysis inside these routes.</action>
  <acceptance_criteria>
    - `rg "application/pdf" src/app/api/export/pdf/route.ts` returns one match.
    - `rg "attachment; filename" src/app/api/export/pdf/route.ts` returns one match.
    - `rg "application/vnd.openxmlformats-officedocument.wordprocessingml.document" src/app/api/export/docx/route.ts` returns one match.
    - `rg "attachment; filename" src/app/api/export/docx/route.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/export/__tests__/pdf.route.test.ts src/app/api/export/__tests__/docx.route.test.ts</automated>
  </verify>
  <done>The app exposes dedicated PDF and DOCX export endpoints that return downloadable files derived from the edited document payload and correct content headers.</done>
</task>

</tasks>

<verification>
Run the export route and exporter tests, then `npm test`, and manually export one documentary report plus one narrative-style document to confirm cover pages, typography, and quote labels survive both formats.
</verification>

<success_criteria>
Users can export the current edited document as PDF or DOCX through dedicated server routes, with consistent cover-page metadata and layout rules that differ appropriately by project type.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-document-generation/04-03-SUMMARY.md`
</output>
