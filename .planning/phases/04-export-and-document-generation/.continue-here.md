---
phase: 04-export-and-document-generation
task: post-completion improvements
total_tasks: 3
status: in_progress
last_updated: "2026-03-18T00:57:22.078Z"
---

<current_state>
Phase 4 is complete and verified. We are in post-v1.0 improvement mode — small UX and feature additions on top of the working app. User has more improvements to add (not yet fully specified).
</current_state>

<completed_work>

- Gap closure (04-06): Fixed TypeScript build errors — Buffer→ArrayBuffer in export routes, narrowed JSX heading tag union — Done
- Bug fix: Fixed `buildRegistry` call in `generators/index.ts` missing API key args — outline/treatment generation now works — Done
- Key security: Moved API keys to `.env.local`, cleared from `settings.json`, env var fallback wired in `settings.ts` — Done
- Model updates: Defaults updated to `gpt-5.4` / `claude-sonnet-4-6`, OpenAI model dropdown with full GPT-4–5.4 list, Anthropic dropdown with Claude 4 models — Done
- Loading state: Generate Outline / Generate Treatment buttons now show spinner + "Generating..." and disable during generation — Done

</completed_work>

<remaining_work>

- User wants to add more things (not yet specified — /gsd:pause-work was invoked mid-conversation)
- User mentioned: review and optimize the site
- User mentioned: "will the next pages be available?" — needs clarification (new pages to build vs existing pages question)

</remaining_work>

<decisions_made>

- API keys stored in `.env.local` only — `settings.json` key fields left empty, backend falls back to env vars
- `gpt-5.4` set as default OpenAI model (confirmed accessible via API)
- Loading state lives in `DocumentWorkspace` as `generatingKind` state — all generate buttons disabled during any generation
- `onGenerateDocument` prop type changed from `void` to `Promise<void>` to enable await

</decisions_made>

<blockers>

- None currently

</blockers>

<context>
v1.0 milestone fully complete and verified. App is working end-to-end: PDF upload → analysis → outline/treatment generation → PDF/DOCX export. OpenAI gpt-5.4 confirmed working. Next session should ask what additional things the user wants to add and clarify the "next pages" and "review/optimize" questions before doing any work.
</context>

<next_action>
Ask user: "What other things did you want to add?" and clarify "next pages" — new pages to build, or something else? Then scope the review/optimize work before starting.
</next_action>
