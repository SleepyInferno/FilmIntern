---
phase: 04-export-and-document-generation
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/app/api/export/pdf/route.ts
  - src/app/api/export/docx/route.ts
  - src/app/api/export/__tests__/pdf.route.test.ts
  - src/app/api/export/__tests__/docx.route.test.ts
  - src/components/document-workspace.tsx
  - src/components/__tests__/document-workspace.test.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - OUTP-02
must_haves:
  truths:
    - "The user can export the currently selected report or generated document as PDF or DOCX from the shared document state."
    - "Export uses the edited active tab content exactly instead of regenerating from analysis data."
    - "The export action remains a single top-right dropdown flow rather than separate format buttons."
  artifacts:
    - path: "src/app/api/export/pdf/route.ts"
      provides: "PDF byte response route"
    - path: "src/app/api/export/docx/route.ts"
      provides: "DOCX byte response route"
    - path: "src/components/document-workspace.tsx"
      provides: "Top-right export dropdown that resolves the active document tab"
    - path: "src/app/page.tsx"
      provides: "Client export wiring from active document selection to browser download flow"
  key_links:
    - from: "src/components/document-workspace.tsx"
      to: "src/app/page.tsx"
      via: "workspace passes the current GeneratedDocument into the page-level onExport callback"
      pattern: "onExport"
    - from: "src/app/page.tsx"
      to: "/api/export/pdf"
      via: "onExport('pdf', activeDocument) POST with { document } then blob download"
      pattern: "/api/export/pdf"
    - from: "src/app/page.tsx"
      to: "/api/export/docx"
      via: "onExport('docx', activeDocument) POST with { document } then blob download"
      pattern: "/api/export/docx"
    - from: "src/app/api/export/pdf/route.ts"
      to: "src/lib/documents/export-pdf.ts"
      via: "route handler calls PDF exporter with request payload"
      pattern: "exportPdf"
    - from: "src/app/api/export/docx/route.ts"
      to: "src/lib/documents/export-docx.ts"
      via: "route handler calls DOCX exporter with request payload"
      pattern: "exportDocx"
---

<objective>
Expose the export pipeline through API routes and wire the active-tab download flow into the page workspace.

Purpose: Finish OUTP-02 by connecting the shared export internals to real user-triggered downloads without regressing the locked single-dropdown export behavior.
Output: PDF/DOCX export routes, route and workspace tests, and page-level browser download wiring that exports the active edited document.
</objective>

<execution_context>
@C:/Users/bplan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bplan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-export-and-document-generation/04-CONTEXT.md
@.planning/phases/04-export-and-document-generation/04-RESEARCH.md
@.planning/phases/04-export-and-document-generation/04-VALIDATION.md
@src/lib/documents/types.ts
@src/lib/documents/export-request.ts
@src/lib/documents/export-pdf.ts
@src/lib/documents/export-docx.ts
@src/components/document-workspace.tsx
@src/app/page.tsx

<interfaces>
From src/lib/documents/types.ts:
```typescript
type ExportFormat = 'pdf' | 'docx';

interface GeneratedDocument {
  id: string;
  kind: DocumentKind;
  projectType: string;
  title: string;
  writtenBy: string;
  createdAt: string;
  updatedAt: string;
  outlineMode?: OutlineMode;
  cover: DocumentCover;
  content: Record<string, unknown>;
  quoteRefs: DocumentQuoteRef[];
  sourceText: string;
  analysisSnapshot?: Record<string, unknown>;
}
```

From src/components/document-workspace.tsx:
```typescript
onExport(format: ExportFormat, document: GeneratedDocument): Promise<void> | void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export route and active-document wiring tests</name>
  <read_first>
    - src/components/document-workspace.tsx
    - src/components/__tests__/document-workspace.test.tsx
    - src/app/page.tsx
    - src/lib/documents/export-request.ts
    - src/lib/documents/export-pdf.ts
    - src/lib/documents/export-docx.ts
    - src/lib/documents/types.ts
  </read_first>
  <files>src/app/api/export/__tests__/pdf.route.test.ts, src/app/api/export/__tests__/docx.route.test.ts, src/components/__tests__/document-workspace.test.tsx</files>
  <behavior>
    - Test 1: POST `/api/export/pdf` returns status `200`, `Content-Type: application/pdf`, and `Content-Disposition` ending in `.pdf`.
    - Test 2: POST `/api/export/docx` returns status `200`, `Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document`, and `Content-Disposition` ending in `.docx`.
    - Test 3: selecting `PDF (.pdf)` or `Word (.docx)` in `DocumentWorkspace` calls `onExport(format, activeDocument)` with the currently selected tab document.
    - Test 4: changing the active tab before export uses that updated `GeneratedDocument` instance rather than a stale report or ID-only payload.
  </behavior>
  <action>Create or extend the route and workspace test files before implementation. The route tests must send the exact body `{ document: GeneratedDocument }`, mock the exporter functions, and assert the route headers exactly `application/pdf` and `application/vnd.openxmlformats-officedocument.wordprocessingml.document` plus `Content-Disposition` filenames ending in `.pdf` and `.docx`. Extend `src/components/__tests__/document-workspace.test.tsx` so the export dropdown path is pinned down before implementation: change the active tab, choose each format option, and assert the callback receives the active `GeneratedDocument` instance rather than a stale report or ID-only payload.</action>
  <acceptance_criteria>
    - `rg "application/pdf" src/app/api/export/__tests__/pdf.route.test.ts` returns one match.
    - `rg "application/vnd.openxmlformats-officedocument.wordprocessingml.document" src/app/api/export/__tests__/docx.route.test.ts` returns one match.
    - `rg "Content-Disposition" src/app/api/export/__tests__/pdf.route.test.ts src/app/api/export/__tests__/docx.route.test.ts` returns at least two matches.
    - `rg "onExport" src/components/__tests__/document-workspace.test.tsx` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/export/__tests__/pdf.route.test.ts src/app/api/export/__tests__/docx.route.test.ts src/components/__tests__/document-workspace.test.tsx</automated>
  </verify>
  <done>The export route contracts and active-document callback behavior are pinned down with executable tests before route and page wiring is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose export routes and wire client download behavior from the active document tab</name>
  <read_first>
    - src/components/document-workspace.tsx
    - src/app/page.tsx
    - src/lib/documents/export-request.ts
    - src/lib/documents/export-pdf.ts
    - src/lib/documents/export-docx.ts
    - src/lib/documents/types.ts
    - src/app/api/analyze/route.ts
  </read_first>
  <files>src/app/api/export/pdf/route.ts, src/app/api/export/docx/route.ts, src/components/document-workspace.tsx, src/app/page.tsx</files>
  <behavior>
    - Test 1: the PDF route accepts a serialized `GeneratedDocument` and returns PDF bytes with a `.pdf` filename.
    - Test 2: the DOCX route accepts a serialized `GeneratedDocument` and returns DOCX bytes with a `.docx` filename.
    - Test 3: the page-level `onExport(format, document)` posts `{ document }` to the matching route, converts the response to a blob, and triggers a file download using the returned format extension.
    - Test 4: export always uses the currently active report or generated document tab, not whichever document was first rendered.
  </behavior>
  <action>Create `src/app/api/export/pdf/route.ts` and `src/app/api/export/docx/route.ts` with `POST` handlers that parse the exact JSON body `{ document }`, validate `document` through the shared `src/lib/documents/export-request.ts` contract, call `exportPdf(document)` or `exportDocx(document)`, and return `new Response(buffer, { status: 200, headers: { 'Content-Type': ..., 'Content-Disposition': 'attachment; filename=\"' + sanitizedTitle + '.pdf\"' } })`. The PDF route must use `application/pdf`; the DOCX route must use `application/vnd.openxmlformats-officedocument.wordprocessingml.document`. Update `src/app/page.tsx` to implement the workspace callback `onExport(format, document)` exactly once at the page level: choose `/api/export/pdf` or `/api/export/docx`, `fetch` with `method: 'POST'`, `headers: { 'Content-Type': 'application/json' }`, and `body: JSON.stringify({ document })`, convert the successful response to a `Blob`, derive the filename from the `Content-Disposition` header when present, and trigger a download in the browser. Keep `src/components/document-workspace.tsx` dumb: it should only pass the currently active document into `onExport` when the user selects a format from the dropdown. Do not regenerate document payloads during export; the active in-memory `GeneratedDocument` is the source of truth.</action>
  <acceptance_criteria>
    - `rg "application/pdf" src/app/api/export/pdf/route.ts` returns one match.
    - `rg "attachment; filename" src/app/api/export/pdf/route.ts` returns one match.
    - `rg "application/vnd.openxmlformats-officedocument.wordprocessingml.document" src/app/api/export/docx/route.ts` returns one match.
    - `rg "attachment; filename" src/app/api/export/docx/route.ts` returns one match.
    - `rg "/api/export/pdf" src/app/page.tsx` returns one match.
    - `rg "/api/export/docx" src/app/page.tsx` returns one match.
    - `rg "JSON.stringify\\(\\{ document \\}\\)" src/app/page.tsx` returns one match.
    - `rg "Blob|createObjectURL|Content-Disposition" src/app/page.tsx` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/export/__tests__/pdf.route.test.ts src/app/api/export/__tests__/docx.route.test.ts src/components/__tests__/document-workspace.test.tsx</automated>
  </verify>
  <done>The app exposes dedicated PDF and DOCX export endpoints and the client can export the currently selected document tab through a real download flow that sends the edited `GeneratedDocument` payload unchanged.</done>
</task>

</tasks>

<verification>
Run the export route and workspace tests, then `npm test`, and manually export one documentary report plus one narrative-style document to confirm the active-tab download flow hits the correct format endpoint and returns files with the expected extensions.
</verification>

<success_criteria>
Users can export the current edited report or generated document tab as PDF or DOCX through dedicated server routes, with correct download handling and the locked single-dropdown export interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-document-generation/04-04-SUMMARY.md`
</output>
